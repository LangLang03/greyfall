# 灰域纪元 GREYFALL · 游戏教程

> 本文介绍当前版本的操作流程、游戏规则、命令与参数。

---

## 目录

0. [运行环境](#0-运行环境)
1. [游戏概述](#1-游戏概述)
2. [一局的进行方式](#2-一局的进行方式)
3. [第一次游玩](#3-第一次游玩)
4. [命令行使用规则](#4-命令行使用规则)
5. [行动点与回合](#5-行动点与回合)
6. [待抉择事件](#6-待抉择事件)
7. [机制：世界与阵营](#7-机制世界与阵营)
8. [机制：市场](#8-机制市场)
9. [机制：贸易路线与关税](#9-机制贸易路线与关税)
10. [机制：外交与情报](#10-机制外交与情报)
11. [机制：道具](#11-机制道具)
12. [机制：间谍网络](#12-机制间谍网络)
13. [机制：线索与推断](#13-机制线索与推断)
14. [机制：国家运营](#14-机制国家运营)
15. [机制：舰队与战斗](#15-机制舰队与战斗)
16. [机制：将领晋升](#16-机制将领晋升)
17. [机制：前线与战线](#17-机制前线与战线)
18. [机制：议会立法](#18-机制议会立法)
19. [机制：科技与政策](#19-机制科技与政策)
20. [机制：决议系统](#20-机制决议系统)
21. [机制：决议的互斥与依赖](#21-机制决议的互斥与依赖)
22. [机制：谈判与提案](#22-机制谈判与提案)
23. [机制：谈判条款](#23-机制谈判条款)
24. [机制：战争理由与战争疲劳](#24-机制战争理由与战争疲劳)
25. [机制：战争迷雾与情报](#25-机制战争迷雾与情报)
26. [机制：人事（领袖 / 科学家 / 集团军）](#26-机制人事领袖--科学家--集团军)
27. [机制：政体差异](#27-机制政体差异)
28. [机制：舰船设计与改造](#28-机制舰船设计与改造)
29. [机制：恒星基地与建筑](#29-机制恒星基地与建筑)
30. [机制：起义与党派斗争](#30-机制起义与党派斗争)
31. [机制：间谍网络的进阶玩法](#31-机制间谍网络的进阶玩法)
32. [机制：种族、奴役、太空生物与基因改造](#32-机制种族奴役太空生物与基因改造)
33. [机制：建造工期、政体与腐败](#33-机制建造工期政体与腐败)
34. [机制：胜利条件](#34-机制胜利条件)
35. [机制：纪元与结局](#35-机制纪元与结局)
36. [机制：存档与读档](#36-机制存档与读档)
37. [命令速查表](#37-命令速查表)

---

## 0. 运行环境

从源码运行时，在项目根目录构建：

```bash
cmake -S . -B build -DCMAKE_BUILD_TYPE=Release
cmake --build build -j
```

**本文命令统一以 `greyfall` 简写，请按程序所在位置替换前缀：**

| 运行位置 | 命令前缀 |
|---|---|
| 当前 Linux 项目根目录 | `./build/greyfall` |
| Linux 可执行文件所在目录 | `./greyfall` |
| Windows PowerShell，可执行文件所在目录 | `.\greyfall.exe` |
| 可执行文件已加入 PATH | `greyfall` |

若使用预编译交付包，按系统选择文件：

| 文件 | 系统 | 说明 |
|---|---|---|
| `greyfall.exe` | Windows 10/11（64 位） | 双击或在 cmd / PowerShell 中运行 |
| `greyfall` | Linux（64 位） | 需 `chmod +x greyfall` |

项目使用 C++20，无第三方库依赖；源码构建需要 C++20 编译器与 CMake。存档默认位置：

| 系统 | 存档目录 |
|---|---|
| Windows | `%APPDATA%\greyfall\saves` |
| Linux | `~/.local/share/greyfall/saves` |

想放到别处（例如放在 U 盘里随身带），设环境变量 `GREYFALL_DIR` 指向目标目录即可：

```
:: Windows
set GREYFALL_DIR=D:\greyfall-data
greyfall.exe status

# Linux
export GREYFALL_DIR=/path/to/data
./greyfall status
```

**Windows 提示**：程序会自动把控制台切到 UTF-8 代码页，因此中文能正常显示。
若在旧版终端里仍见乱码，先执行 `chcp 65001` 再运行。

**随时可以中断**：每条命令执行完都会自动存档，直接关掉窗口即可，下次继续。

---

## 1. 游戏概述

你扮演一个星际阵营的领袖（阵营序号 0），国名、种族与政体随开局生成。
在 7 幕主线剧情中，通过**市场交易、外交博弈、情报活动、内政运营**推进局势。
游戏包含 12 种剧情结局，另有完成征服并通过连续治理考验的征服结局。

**三个需要提前知道的特点：**

| 特点 | 含义 |
|---|---|
| **纯命令行** | 没有图形界面，也没有交互式提示符。一条命令 = 一步操作，命令执行完即退出。 |
| **对手会读你的数据** | 默认 11 个 AI 对手（总帝国数包含玩家）能直接看到你的库存、线索、研发进度、舰队坐标、未成交订单、读档次数。你无法隐藏数据，只能**布置数据**。 |
| **读档会被记录** | 回退进度会被 AI 察觉，并影响它们后续对你的态度。 |

---

## 2. 一局的进行方式

**时间单位：tick（1 tick = 1 季）。**

一局的循环是：

```
看状态 → 发命令（下单 / 外交 / 运营 / …）→ 推进 tick → 看结果
```

有两个限制需要留意：

1. **每季能做的事有限**，由「行动点（AP）」约束。见第 5 节。
2. **有时游戏会要求你先做选择**，不选就无法继续推进。见第 6 节。

推进 tick 用 `advance` 命令。推进后，市场成交、AI 行动、外交变化、事件、战斗等都会结算。

---

## 3. 第一次游玩

按顺序执行即可完成一局的开场。

### 第 1 步：开一个新局

```bash
greyfall new --seed 5EED-C0FFEE --difficulty 3 --empires 12
```

| 参数 | 说明 |
|---|---|
| `--seed <S>` | 随机种子。可用十六进制串（`5EED-C0FFEE`）或任意文字。同一版本、相同种子与开局参数生成相同世界。省略则按当前时间生成种子。 |
| `--difficulty <1..5>` | 难度。默认 2，最高 5；影响 AI 前瞻与军事加成，难度 5 的军事倍率为 ×1.72。 |
| `--empires <8..16>` | 帝国总数，包含玩家。默认 12，最多 16（玩家 + 15 个 AI）。 |
| `--systems <24..256>` | 星系数量，最大 256。默认 `56 + 帝国数 × 2`，12 个帝国时为 80；超出范围的输入会截到边界值。 |
| `--endless` | 无尽模式：达成结局后不终止纪元。 |
| `--force` | 覆盖已存在的存档槽。 |

存档槽默认叫 `main`。若该槽已存在，`new` 会拒绝执行，需加 `--force` 或换槽（`--slot <名字>`）。

**最高难度、最大地图、最多帝国开局**（在当前 Linux 项目根目录直接执行）：

```bash
./build/greyfall new --difficulty 5 --systems 256 --empires 16 --slot max
```

这会在独立的 `max` 槽新建随机世界：**难度 5、256 个星系、16 个帝国**。
行星数随生成结果变化，`--systems` 控制的是星系数。后续操作也要指定同一个槽：

```bash
./build/greyfall status --slot max
./build/greyfall starmap --slot max
./build/greyfall advance --ticks 1 --slot max
```

下文其他示例使用默认 `main` 槽；用于这局时加上 `--slot max`。

### 第 2 步：看状态

```bash
greyfall status
```

输出包含：回合数、纪元、当前幕、难度、帝国名、国库、国力指数、行动点、待抉择事件数、星系/行星数、舰队数、民怨/合法性、信用评级、读档次数、活跃帝国数。

**其中「行动点」决定你这季还能做几件事。未处理且未延后、或延后已到期的抉择会阻止推进；尚未到期的延后事件仍可能列在待抉择列表中。**

### 第 3 步：看市场

```bash
greyfall market --res alloys --depth 8
```

看某个商品（这里是合金）在三个交易所的买一/卖一/中间价/价差，以及核心交易所的档位深度。

### 第 4 步：下单

```bash
greyfall order buy alloys 4200 @31.4 --tif gtc
```

含义：买入 4200 单位合金，限价 31.4，有效期到撤销为止。

下单不消耗行动点。

### 第 5 步：推进

```bash
greyfall advance --ticks 1
```

`--ticks N` 可一次推进多季（默认 1）。加 `--dry-run` 可以预演结果，不写入存档或操作记录链。

### 第 6 步：处理待抉择

如果 `advance` 返回**退出码 5**，说明出现了需要你选择的事件：

```bash
greyfall choose 0        # 只看第 0 个事件的选项
greyfall choose 0 1      # 选第 1 个选项
```

或延后：

```bash
greyfall defer 0 --ticks 2
```

### 第 7 步：随时查看全景

```bash
greyfall overview        # 世界总览
greyfall empires         # 所有阵营
greyfall domestic        # 你的国内派系
greyfall logs --last 20  # 最近 20 条事件日志
```

---

## 4. 命令行使用规则

### 4.1 两种等价写法

```bash
greyfall advance --ticks=3
greyfall --action=advance --ticks=3
```

位置参数（如 `buy`、`alloys`、`4200`）与选项（`--tif gtc`）的书写顺序不影响结果。

### 4.2 选项写法

- `--tif gtc` 与 `--tif=gtc` 等价。
- 布尔选项（如 `--verbose`、`--net`、`--yes`）直接写，后面不跟值。

### 4.3 全局选项

这些选项可加在任意命令后：

| 选项 | 说明 |
|---|---|
| `--slot <id>` | 指定存档槽（默认 `main`） |
| `--data-dir <path>` | 指定数据目录 |
| `--key <口令>` | 存档加密口令 |
| `--quiet` | 静默模式 |
| `--verbose` | 详细模式（逐 tick 明细；写两次显示更多） |
| `--no-autosave` | 本次不写存档（纯查询用） |
| `--dry-run` | 采样评估，不写存档 |
| `--ascii` | 纯 ASCII 输出（同样可用 `NO_COLOR` 环境变量） |
| `--no-encrypt` | 开发模式：存档不加密 |

### 4.4 退出码

| 码 | 含义 |
|---|---|
| `0` | 成功 |
| `1` | 参数错误 |
| `2` | 存档不存在 |
| `3` | 存档损坏或口令错误 |
| `4` | 动作不被允许（行动点不足、条件未满足、资金不足等） |
| `5` | 存在待抉择事件，推进被中止 |
| `6` | 存档版本与程序不兼容 |
| `7` | 市场未成交 |
| `70` | 内部错误 |

在脚本中可据此判断：

```bash
greyfall advance --ticks 4
case $? in
  0) echo "推进完成" ;;
  5) echo "有事件要处理"; greyfall choose 0 ;;
esac
```

---

## 5. 行动点与回合

每季开始时行动点复位。上限见 `status` 输出的「行动点 x / y」。

### 消耗表

| 行动 | 命令 | 消耗 |
|---|---|---|
| 下单 / 撤单 / 改单 | `order` `cancel` `modify` | 0 |
| 期货开仓 | `futures` | 0 |
| 还款 / 借款 | `borrow` `repay` | 0 |
| 托管 / 保险 / 交割 | `escrow` `insure` `settle` | 0 |
| 合成 / 使用道具 / 拆解 | `combine` `use` `disassemble` | 0 |
| 连接 / 断开线索 | `link` `unlink` | 0 |
| 外交使节 | `envoy` | 1 |
| 赠礼 | `gift` | 0 |
| 舆论战 | `propaganda` | 1 |
| 间谍行动 | `spy` | 2 |
| 造假冒牌 | `forge-prove` | 1 |
| 殖民 | `colony` | 2 |
| 建造舰船 | `ship-build` | 1 |
| 舰队命令 | `fleet` | 1 |
| 颁布法令 | `edict` | 1 |
| 研究 | `research` | 1 |
| 建造建筑 | `build` | 1 |
| 巨构工程（每阶段） | `mega` | 见命令提示 |
| 飞升 | `ascend` | 3 |
| 招募 | `recruit` | 1 |
| 推断结论 | `deduce --commit` | 2 |
| 谈判 | `negotiate` | 1 |
| 发起决议 | `resolve --activate` | 见决议详情（1–3） |

行动点不足时命令返回**退出码 4**。

---

## 6. 待抉择事件

游戏中会不断出现需要你决策的事件（危机、外交照会、异常发现、剧情节点、国内派系诉求）。

**存在未延后或延后已到期的未处理抉择时，`advance` 会中止并返回退出码 5。**

### 查看与选择

```bash
greyfall status                    # 查看待抉择列表及序号
greyfall choose 0                  # 查看第 0 个事件的正文与选项
greyfall choose 0 1                # 选第 1 个选项
greyfall choose 0 --option 1        # 同上，另一种写法
greyfall defer 0 --ticks 2          # 把第 0 个事件延后 2 季
```

**查看时要写上序号**：`choose 0` 只显示选项；直接运行不带参数的 `choose` 会执行第 0 个事件的第 0 个选项。

`defer` 的延后季数为 1～400。每延后 1 季立即增加 2 个百分点民怨，并降低其他帝国观感。
延后期限内，这一事件不阻止推进；到期后必须处理或再次延后。多季推进也会在途中遇到新的阻塞事件时停止。

国内派系诉求的选项为**满足、镇压、暂不处理**。满足需支付费用，资金不足时不会消除诉求；
镇压会损害派系满意度、民怨与合法性。这里的「暂不处理」会结算该诉求并增加压力，与 `defer` 保留事件、推迟期限不同。
通过 `choose` 回应诉求不额外消耗行动点；主动颁布 `edict 满足军部` 等法令消耗 1 AP。

---

## 7. 机制：世界与阵营

### 7.1 地图三层结构

| 层级 | 说明 | 查看命令 |
|---|---|---|
| 星区 | 若干星系的集合，有开发度 | `sectors` |
| 星系 | 地图节点，有归属、航线、封锁度、异常点 | `starmap` `starmap --system <id>` |
| 行星 | 星系内的可殖民星球，有类型、规模、人口、产出 | `planet <id>` |

`starmap` 用字符表示地图：

| 符号 | 含义 |
|---|---|
| `O` | 我方星系 |
| `o` | 无主星系 |
| `*` | 我方首都 |
| `x` | 他国星系 |
| `?` | 异常点 |
| `@` | 巨构 |
| `¤` | 封锁中 |
| `\|` | 航线 |

### 7.2 阵营属性

每个阵营（含你自己）有：

- **国力指数**：综合实力评分
- **国库**（credits）
- **军事 / 经济**
- **稳定度 / 合法性**
- **信用评级**：影响借款利率与额度
- **影响力 / 凝聚力**
- **资源**：22 类，包含信用点与 21 种可交易商品
- **科技进度**
- **舰队与舰船设计**
- **国内派系**
- **对他国的观感**

查看：`empires`、`relations`、`fleets`、`tech`、`ship`

### 7.3 国内派系

你的帝国内部有 8 个派系：军部、商会、技术官僚、原教旨、劳工、旧贵族、民粹、辛迪加。

每个派系有：

- **影响力**：它对政局的分量
- **满意度**：低于阈值会开始累积**诉求压力**
- **诉求**：压力足够高时提出具体要求，进入待抉择队列
- **外部资助者**：AI 阵营可能资助你的派系（代理人战）

```bash
greyfall domestic
```

应对方式（都消耗行动点）：

```bash
greyfall edict 满足军部      # 花国库满足诉求
greyfall edict 边境戒严      # 支付资金及每季维护，逐季改善稳定与民怨
```

**民怨过高会触发政变**：`domestic` 输出中的「政变风险」超过阈值后进入倒计时，倒计时结束会重创国库、军力与合法性。

### 7.4 联邦

多个阵营可以组成联邦，通过加权投票决定共同事务（接纳成员、共同舰队、税收协调、制裁、宣战等）。

```bash
greyfall federation          # 查看成员、凝聚力、动议与投票权重
```

参与投票：

```bash
greyfall envoy <emp> vote yes --motion <动议号>
```

加入 / 退出：

```bash
greyfall envoy <emp> federate-join
greyfall envoy <emp> federate-leave
```

### 7.5 条约与战争

```bash
greyfall treaties            # 当前所有生效条约
greyfall relations           # 关系矩阵
greyfall relations --empire 3
```

条约种类：贸易协定、互不侵犯、防御同盟、研究协定、联合情报、联邦、宗藩、战争、制裁、封锁、停火、索贡。

---

## 8. 机制：市场

### 8.1 三个交易所

| 代号 | 名称 | 特点 |
|---|---|---|
| `CX` | 核心区环币交易所 | 深度最好，手续费最低，监管最严 |
| `FX` | 边疆自由市场 | 价格偏高，运费高，监管松 |
| `BZ` | 黑市 | 价格最高，有查没风险 |

三个市场的价格互不相同，`arb` 命令可以找出差价。

### 8.2 可交易标的

**21 种可交易现货**（命令中用小写英文名；信用点用于支付，不作为现货下单）：

| 命令名 | 中文 | 命令名 | 中文 |
|---|---|---|---|
| `energy` | 能源 | `luxury` | 奢侈品 |
| `minerals` | 矿物 | `volatiles` | 挥发物 |
| `food` | 食物 | `raregases` | 稀有气体 |
| `alloys` | 合金 | `polymers` | 聚合物 |
| `components` | 部件 | `electronics` | 电子元件 |
| `medicines` | 药品 | `robotics` | 机器人 |
| `supermaterials` | 超材料 | `bioproducts` | 生物制品 |
| `exotic` | 异物质 | `antimatter` | 反物质 |
| `datacrystals` | 数据晶 | `relics` | 遗物 |
| `influence` | 影响力 | `contraband` | 违禁品 |
| `unity` | 凝聚力 | | |

**4 档期货**：`F1` `F2` `F3` `F4`。每 4 季安排一批交割，`F1` 是当前批次，
`F2`～`F4` 分别往后顺延 1～3 批。距离交割还有多久取决于当前季数。
例如第 0 季开仓时，四档分别在第 3、7、11、15 季到期；第 5 季开仓的 `F1` 在第 7 季到期。

查看期货曲线：

```bash
greyfall curve alloys
```

### 8.3 订单类型

`order` 命令支持三种单型（`--kind`）：

| 类型 | 说明 |
|---|---|
| `limit` | 限价单。指定价格，达不到就不成交；给出价格时默认使用 |
| `market` | 市价单。立即以对手方价格成交；**未成交部分不入订单簿** |
| `iceberg` | 冰量单。只向市场展示一部分数量（`--shown N`），隐藏真实总量 |

未给价格、也未指定 `--kind` 时，按市价单处理。冰量单仍受限价约束；展示量可以补充，单次撮合的总成交量可能超过 `--shown`。

**有效期**（`--tif`）：

| 值 | 说明 |
|---|---|
| `gtc`（默认） | 一直有效，直到成交或被撤销 |
| `day` | 只在本季有效，季末自动撤销 |

完整参数：

```bash
greyfall order buy|sell <商品> <数量> [@ <价格>] [--exch CX|FX|BZ] [--tif day|gtc] [--kind limit|market|iceberg] [--shown N]
```

示例：

```bash
greyfall order buy alloys 4200 @31.4 --tif gtc          # 限价买单
greyfall order sell minerals 1000 --kind market         # 市价卖单
greyfall order buy food 500 @12.0 --kind iceberg --shown 100   # 冰量单，只露 100
```

**价格约束**：限价单和冰量单价格不得偏离该商品中间价 ±30%，超出会被拒绝。

**成交顺序**：买单先成交最低卖价，卖单先成交最高买价；同价位按挂单先后成交，不会与自己成交。

每笔成交都会结算双方的货款、库存与手续费，包括挂单随后被其他人吃单的情况。
现货卖出必须有库存，买入必须付得起货款和费用。
挂单不会预先冻结资金或库存；成交时会重新检查可用余额，余额不足的剩余委托会缩量或撤除。
同一笔资金或库存不能同时支撑多笔成交。

做市商也有有限库存，库存压力会影响报价量与价差。重报价不会补满库存，
库存回归按季结算；连续交易后应重新查看盘口。

### 8.4 撤单与改单

```bash
greyfall cancel <订单号>                       # 撤销订单
greyfall modify <订单号> @ <新价格> <新数量>    # 修改
```

订单号在下单成功的输出中给出。改单会失去原有的排队优先权。

### 8.5 查看盘面

| 命令 | 用途 |
|---|---|
| `market --res <商品>` | 三所对照 + 档位深度 |
| `book CX alloys --depth 8` | 单个交易所的订单簿 |
| `quote alloys` | 三所最优买卖价 |
| `curve alloys` | 期货各期限价格与基差 |
| `vol alloys` | 波动率状态与近期冲击 |
| `arb` / `arb --net` | 跨所套利机会（`--net` 扣除费用后） |
| `shock --last 10` | 近期市场冲击记录 |
| `fx` | 汇率与配给 |
| `blackmarket` | 黑市价格与风险 |

### 8.6 持仓、盈亏与保证金

```bash
greyfall position     # 现货与期货持仓、均价、保证金占用
greyfall pnl          # 已实现/未实现盈亏
greyfall pnl --ticks 20
```

- 期货可加 1～20 倍杠杆（`--lev n`）。
- 账户权益包含现金、持仓占用的保证金、未实现盈亏与计入账户的现货价值；开仓占用保证金不等于亏掉这笔钱。
- 亏损过大会触发**追保**，进一步触发**强制平仓**，可能影响市场价格。
- 保证金余量在 `position` 中显示，低于 1 时进入危险区。

### 8.7 期货交易

```bash
greyfall futures buy|sell <商品> <F1|F2|F3|F4> <数量> [--lev n]
```

示例：

```bash
greyfall futures sell alloys F3 9000 --lev 3
```

`buy` 开多仓，`sell` 开空仓；反向开仓不会自动平掉已有仓位。
`advance` 会在到期时自动交割并释放保证金，盈亏计入国库。也可执行：

```bash
greyfall settle
```

`settle` 处理当前已到期的期货与债务，不推进时间，也不会提前交割尚未到期的合约。
`position` 和 `pnl` 显示自己的账户，其他帝国的仓位由各自承担盈亏。

### 8.8 借款、还款、托管、保险

```bash
greyfall credit                              # 查看各阵营信用评级、借贷利差、债务
greyfall borrow 50000 --term 8               # 借款 50000，期限 8 季
greyfall repay --amount 20000                # 还款
greyfall repay                               # 还清全部
greyfall escrow open <emp> <金额> --term 4    # 开立托管（降低对手违约风险）
greyfall escrow release <托管号> <金额>       # 释放托管
greyfall insure alloys --cover 5000          # 为持仓投保
```

**信用评级影响借款利率与额度**；违约会进一步降低评级。

投保时立即扣除保费，`--cover` 默认 1000，数量必须为正，且不能超过尚未投保的实际库存。
保单从投保当季起有效四季，例如第 0 季投保，到第 4 季失效。
权益转负且仍持有承保资源时，按自投保起的跌价损失赔付，最多补足当时的权益缺口。
同一损失只赔一次，累计赔付不超过投保名义额与 50,000 cr 的较小值。

### 8.9 市场操纵与监管

某些行为会被监管机构检测并处罚（罚没资金、信用评级下降）：

- 大量挂单后撤销
- 自买自卖
- 囤积现货
- 先拉抬后抛售
- 利用未公开信息交易

`logs --phase market` 中可以看到相关的检测与裁罚记录。

**你的订单队列对 AI 完全可见**，它们可能抢在你之前成交。可用的规避手段：拆成多笔小额、使用冰量单、用 `--tif day`、或先发布误导信息。

---

## 9. 机制：贸易路线与关税

### 9.1 基本概念

**贸易路线**是帝国之间的实物贸易流：把**出口方的持续产出剩余**运往**进口方**。

| | 获得 | 付出 |
|---|---|---|
| 出口方 | 货款 | 物资 |
| 进口方 | 物资 + 关税收入 | 货款（含税，关税回到自己手里） |

判据是**流量**，不是存量：只有「每季产出 > 每季需求」才是真正可出口的剩余。
光是库存高并不代表能出口 —— 那可能只是正在被消耗的储备。

产出预测会计入行星开发度、稳定度、库存对生产的抑制，以及资源建筑的产出。
每季先检查进口方的付款能力，再交付对应数量的货物；没有可用资金时不会收到免费货物。

### 9.2 查看贸易

```bash
greyfall trade                  # 总览：世界贸易量、我方进出口、关税态势
greyfall trade --detail 3       # 单条路线详情
```

输出含：世界贸易总量、我方进出口量、关税收入、出口收入，
以及**进口路线**（我方征税）与**出口路线**两张列表。

路线状态会标注中断原因：`交战中` / `禁运中` / `无剩余或无缺口` / `进口方无力支付`。

### 9.3 开通与关闭

```bash
greyfall trade --open 1:机器人:12     # 从帝国1 进口机器人，关税 12%
greyfall trade --close 3              # 关闭路线 #3
```

开通条件：双方**不处于战争状态**、无禁运、出口方确有剩余、进口方确有缺口、
且该商品尚无路线。

### 9.4 关税与关税战

```bash
greyfall trade --tariff 3 45     # 把路线 #3 的关税提高到 45%
```

- 只有**进口方**可以调整关税率，范围 **0~60%**。
- 关税率越高，**运力越低**：0% 全额，35% 约剩 55%，60% 约剩 20%。
- 大幅提税会**恶化双方关系**。
- 对方可以对你的高关税实施**报复性关税**（AI 会自动这样做）。
- 结果：双方贸易量崩塌、各自受损 —— 这就是关税战的代价。

### 9.5 实体路径与封锁

每条路线都有**经过星系的实体路径**，连接出口方与进口方的首都。
每季按沿途风险重新选择航线，可能绕路以避开战区或严重封锁。

```bash
greyfall trade --detail 2
```

输出含路径长度、路径风险、以及完整航线：

```
  路径          9 跳
  路径风险      6%
  路线          奥密脊-9 → 纽港-53 → 西塔环-51 → 卡帕垠-35 → …
```

**路径风险**来自沿途星系：

| 因素 | 影响 |
|---|---|
| **封锁**（舰队阻断） | 每节点最高 +60% 风险 |
| 海盗活动 | +30% 风险 |
| 航行风险 | +20% 风险 |
| 贸易枢纽度 | 按比例**抵消**上述风险 |
| 中立国恶劣关系 | 按观感增加风险 |

风险按路径长度开方摊薄（长路线单点风险影响较小，但暴露机会更多），最终按比例削减运力。

**与贸易任一方交战的第三国星系不能通行**。存在其他可通行航线时会自动改道；无路可走时当季停运。

### 9.6 用舰队执行封锁

```bash
greyfall fleet --id 2 --order blockade --to 36
```

执行**阻断**命令的舰队会**每季维持**所在星系的封锁强度：

- 强度由舰队战力决定，上限 **80%**
- 建立需要时间，封锁强度逐季向舰队能维持的目标值靠拢
- **撤离后每季消退 20%**

部署后用 `trade --detail <路线号>` 查看路线是否改道，以及风险、运量的变化。

### 9.7 中断与约束

| 情况 | 结果 |
|---|---|
| 双方进入战争 | 路线自动中断 |
| 任一方实施禁运 | 路线自动中断 |
| 进口方国库不足 | 运量自动缩减到可支付额度 |
| 连续超过 12 季无货可运 | 路线自动关闭 |

**AI 不会替你决定进口** —— 只有你自己用 `--open` 开通的路线才会让你付款。
这样你的国库不会被未经同意的贸易掏空。

---

## 10. 机制：外交与情报

### 10.1 关系属性

`relations` 显示每对国家之间的：

- **观感**（-1 到 1）
- **信任**
- **恐惧**
- **债务**
- **战争状态**与**封锁状态**

### 10.2 外交动作

```bash
greyfall envoy <emp> <动作> [--terms ...]
```

| 动作 | 说明 |
|---|---|
| `propose-trade` | 提议贸易协定 |
| `propose-pact` | 提议互不侵犯 |
| `breach` | 撕毁条约（立即获得好处，但全体观感大幅下降） |
| `demand-tribute` | 索取贡赋 |
| `joint-intel` | 联合情报交换 |
| `sanction` | 实施制裁 |
| `declare-war` | 宣战 |
| `plead-peace` | 求和 |
| `vassalize` | 要求称臣 |
| `federate-join` | 申请加入联邦 |
| `federate-leave` | 退出联邦 |
| `vote yes\|no --motion <N>` | 在联邦动议中投票 |

`<emp>` 可用阵营编号或全名。

接受与否取决于对方的观感、实力对比、以及你的历史记录。

### 10.3 赠礼

```bash
greyfall gift <emp> <商品> <数量>
```

赠礼会提升对方观感。注意：赠礼本身也是一个会被对方读取的信号。

### 10.4 间谍行动

```bash
greyfall spy <emp> --mission <任务> --budget <预算>
```

| 任务 | 作用 |
|---|---|
| `intel` | 获取对方详细情报（国库、军力、资产、态度） |
| `read-mind` | 读出对方对你的心智模型（它如何评估你） |
| `insider` | 侦察对方的内幕交易痕迹 |
| `exfil` | 窃取线索 |
| `sabotage` | 破坏对方军力与国库 |
| `forgery` | 向对方投放伪造情报 |

预算越高成功率越高、收获越大。**失败会被对方察觉**，导致观感下降。

部分任务需要持有特定类型的道具才能执行，缺少时命令会提示。

### 10.5 情报总览

```bash
greyfall intel                          # 所有 AI 对你的观感、威胁、模型置信度
greyfall intel --threat --empire 1      # 某个 AI 对你的威胁评估
greyfall intel --what-they-know --empire 1   # 它究竟看到了你的什么
greyfall intel --counterintel           # 各 AI 对你的了解程度与已识别的行为模式
greyfall intel --decoy --signal "文本"   # 布置假数据，误导 AI 的判断
```

`--what-they-know` 会逐字段列出：AI 读到了什么、通过什么渠道、置信度多少、哪些字段被你布置过。

### 10.6 舆论战

```bash
greyfall propaganda <目标阵营> <叙事文本>
```

影响第三方对目标的观感。**有被识破的风险**，识破后你自己也会受损。

---

## 11. 机制：道具

### 11.1 道具与标签

道具是外交、市场、情报行动的筹码。每件道具有：

- **名称与编号**（可用于命令）
- **层级**（tier 1–5）
- **标签**：契约、观测、伪造、审计、屏障、市场干预、信念干预、线索桥、载体、金融、医疗、军用、数据、遗物、违禁
- **效果数值**
- **来源**：从哪条渠道获得（影响可信度）
- **是否被伪造过**

同时持有特定标签组合会产生**协同**（增益）或**互斥**（减益）。

```bash
greyfall inventory                    # 查看持有（含规则结算汇总）
greyfall inventory --filter 契约       # 按标签筛选
greyfall item 5                       # 查看某件道具
greyfall item 5 --explain             # 查看它的完整结算过程
greyfall item                         # 列出道具表
```

### 11.2 操作

```bash
greyfall combine <A> <B>              # 合成（可能失败，材料会损耗）
greyfall disassemble <道具>            # 拆解回材料
greyfall use <道具> [--target <阵营>]   # 使用
greyfall forge-prove <道具>            # 造假冒牌，用于污染 AI 的判断
greyfall equip <模块> <舰队>           # 给舰队装配模块
```

**造假冒牌是双刃剑**：它能误导 AI，但伪造痕迹本身也会被察觉。

### 11.3 门槛

部分行动需要持有特定标签的道具才能执行（例如某些间谍任务需要「屏障」类道具）。
缺少时命令会返回退出码 4 并说明原因。

---

## 12. 机制：间谍网络

### 12.1 与「间谍行动」的区别

| | `spy`（行动） | `network`（网络） |
|---|---|---|
| 性质 | 一次性，花预算立刻换取情报 | **持续性**基础设施 |
| 需要 | 无前置 | 先在目标国建网 |
| 风险 | 无累积风险 | 暴露累积，**被破获会损声望与关系** |

### 12.2 建立与查看

```bash
greyfall network --establish 1      # 在帝国1 建立网络（5000 cr，占用 1 名特工）
greyfall network                    # 情报机构总览
greyfall network --detail 1         # 单个网络详情
```

开局每个帝国拥有 **2 + 难度** 名特工。特工总数固定，派驻到网络后不可复用（撤销可回收）。

### 12.3 渗透度

渗透度（0~100%）**每季自动增长**，速度取决于：

- **派驻特工数**（越多越快）
- **我方的操纵技巧**修正
- **对方的情报防御**（越高越慢）

用 `--agents` 增派或撤回特工：

```bash
greyfall network --agents 1 2       # 向帝国1 的网络增派 2 名特工
greyfall network --agents 1 -1      # 撤回 1 名
```

### 12.4 任务

| 任务 | 最低渗透度 | 消耗 | 经费 | 效果 |
|---|---|---|---|---|
| 侦察 | 10% | 5% | 200 | 获得目标国力/军力/经济 |
| 煽动 | 25% | 10% | 600 | 目标民怨 +9% |
| 窃取科技 | 40% | 14% | 800 | 获得研究点 |
| 破坏 | 55% | 18% | 1,200 | 目标经济 -6%、稳定度 -4% |
| 潜伏 | 65% | 8% | 1,500 | 渗透度 +18% |

```bash
greyfall network --mission 1 侦察
```

任务有成功率（渗透度越高、对方反间谍越弱则越高）。**失败也会提高暴露**。

### 12.5 暴露与破获 —— 核心权衡

- **特工越多，渗透越快，但暴露也越快。**
- **不执行任务时暴露会自然降温** —— 安静的网络可以长期存活。
- 每次任务都会带来**暴露尖峰**，频繁行动会让网络很快被捣毁。
- 暴露达到 **100%** 即被破获：网络作废、特工回收、**声望 -8%**、**双方关系 -25%**。

特工数量、任务频率与对方情报防御共同影响网络能维持多久。
用 `network --detail <帝国编号>` 查看渗透度与暴露状态，再决定执行任务、潜伏或撤网。

### 12.6 撤销

```bash
greyfall network --disband 1        # 撤销网络，回收全部特工
```

在暴露过高时主动撤网，是保全特工与声望的合理选择。

---

## 13. 机制：线索与推断

### 13.1 线索

每条线索有：

- **可信度**（0–1）：随时间和新证据变化
- **来源**：直接观测、订单流、间谍网络、截获、流言、伪造、对方给予、档案、推演、证词、泛视网络
- **所属幕**
- **连接状态**：是否与其他线索相连

```bash
greyfall clues                        # 全部已知线索
greyfall clues --unlinked             # 只看孤立的
greyfall clues --tag 沉默              # 按标签筛选
greyfall archive <线索>                # 归档（移出工作集）
```

线索可用编号（`137`）、`#137` 或 `clue137` 引用。

### 13.2 连接类型

```bash
greyfall link <A> <B> --kind <类型>
greyfall unlink <A> <B>
```

| 类型 | 含义 |
|---|---|
| `corroborate` | 印证 |
| `contradict` | 矛盾 |
| `prereq` | 前置 |
| `belongs` | 归属 |
| `about` | 指涉主体 |

**矛盾**需要被裁定：可信度差异足够大时自动判定胜负，败方可信度下降。

### 13.3 推断结论

```bash
greyfall deduce                              # 列出本幕所有结论
greyfall deduce --conclusion C-42            # 查看某个结论的详情
greyfall deduce --conclusion C-42 --explain  # 查看完整的推断报告
greyfall deduce --conclusion C-42 --commit   # 提交该结论
greyfall conclusions                         # 查看已提交的结论与结局向量
```

`--explain` 会显示：

- 该结论需要满足的条件（若干条线索组合）
- 所有满足条件的**线索组合**
- 每个组合是否满足附加要求：**来源够不够多样**、**有无未裁定的矛盾**、**是否有来自对手内部的信息**
- 当前是否可以提交

**提交结论有真实后果**：

- 提交**正确**结论：相关市场发生价格变动，AI 重新评估你的意图
- 提交**错误**结论：进入假剧情分支、外交信誉下降、市场恐慌

`--explain` 不会产生任何后果，可以反复查看。已经提交的结论不能重复提交或重复获得奖励。

---

## 14. 机制：国家运营

### 14.1 殖民

殖民是一项需要持续补给的工程。先查看容量和目标报价，再筹集资金与物资：

```bash
greyfall starmap                   # 查找邻接星系及编号
greyfall colony                    # 查看当前容量、主义和科技贡献、已有殖民地维护费
greyfall colony --detail 21        # 只查看目标 21 的报价与工期，不花钱或 AP
greyfall colony 21                 # 支付启动费用，消耗 2 AP
greyfall queue                     # 查看剩余工期、每季维护与停工原因
greyfall advance --ticks 1         # 推进一季
greyfall queue --cancel-project 0  # 取消 queue 中项目号为 0 的国家工程
```

目标须为与**已拥有领土相邻**的无主星系，或本国仍有空闲行星的星系。在建星系不能作为继续扩张的跳板。每次只选择一颗宜居度最高的合适行星建立据点，同一星系同一时间只能有一国的一项殖民工程。气态巨行星、小行星带和碎裂世界不能用此命令定居；宜居度低于 20% 需要先完成「人工生态圈」。

**启动费用**同时检查、一次付清：

| 项目 | 数量 |
|---|---|
| 信用点 | `(18000 + 行星规模 × 600 + 距首都跳数 × 2000) × 殖民费用倍率`，向上取整 |
| 合金 | 300 |
| 部件 | 180 |
| 能源 | 600 |
| 食物 | 500 |
| 药品 | 100 |

殖民费用倍率是 `1 + 殖民费用修正`，限制在 50%～200%。折扣只影响启动资金，物资和维护费照付。任一资源不足均拒绝启动，不会先扣走其他物资。

**工期与施工补给**：基础工期为 `8 + 距首都跳数 + floor((1 − 宜居度) × 10)` 季，再除以建造速率倍率，向上取整。建造速率倍率限制在 50%～200%，最终工期至少 6 季。工期在启动时确定。每个正常施工季度另付 `500 + 距首都跳数 × 100` cr，以及能源 20、食物 15、药品 2、部件 2。

例如，规模 10、距离 1 跳、宜居度 80%，费用和速度均无修正时：启动 26,000 cr，工期 11 季，施工维护共 6,600 cr；还要备好启动物资和 11 季补给。实际数值以 `colony --detail` 为准。

**殖民容量**分为「同时在建」和「非首都星系管理容量」，基础分别为 1 项、6 个星系：

| 来源 | 同时在建 | 管理容量 |
|---|---:|---:|
| 扩张伦理 | +1 | +4 |
| 孤立伦理 | −1 | −2 |
| 集体主义伦理 | — | +2 |
| 蜂群 / 社会主义政体 | +1 | +4 |
| 法西斯主义政体 | — | +3 |
| 企业 / 资本主义 / 技术官僚 / 民主主义政体 | — | +2 |
| 每项已完成社会科技 | 每 8 项 +1 | 每项 +1 |
| 已完成工程科技 | 每 8 项 +1 | 每 4 项 +2 |

同时在建最终限制为 1～5 项，管理容量限制为 3～48 个非首都星系。未完工的无主星系殖民项目预占管理名额；在已有星系增加据点不额外占星系名额。征服所得领土也计入管理规模，超额领土保留，但不能再启动新增星系殖民，且殖民行政维护费每超额一个星系增加 5%，最高增加 300%。

**建成后的经营**：完工时才取得星系与一处 200k 人口据点，从下一季开始产出。新据点的自然产量从成熟产量的 25% 起步，在随后 12 季逐步升至 100%；同星系其他行星需要另行投资殖民。

每个非首都星系每季行政维护为 `80 + 已定居行星数 × 20 + 距首都跳数 × 15` cr，失去首都航线连接时按全图星系数计算运输距离。每颗已定居行星另消耗能源 8、食物 5、药品 1。贸易需求与胜利所需物资储备会计入这些日常需求。药品可通过贸易补充，也可建设「生物医学中心」稳定生产（每座每季 75 药品，维护 220 cr，需满足建筑前置科技）。

**停工与取消**：资金、补给不足或目标遭到至少 50% 封锁时不推进；殖民还要求有未严重封锁的邻接本国星系提供补给。在建领土失守、据点被占用、管理容量耗尽也会停工。停工期间收取正常施工维护的 20% 留守费，最多收至国库归零，不扣施工物资；连续停工 8 季自动撤销且不退款。主动取消只返还启动资金的 40%，物资、凝聚力、影响力和已付维护不退。

这些施工、停工和取消规则也适用于巨构阶段、恒星基地、飞升、招募训练、基因改造、罐头加工与迁都；具体项目的资源、工期和并行限制见下文。工程维护计入当季经营净收入。玩家和 AI 使用相同的殖民与巨构施工规则。

### 14.2 舰船

```bash
greyfall ship                              # 列出你的舰船设计
greyfall ship <设计名或编号>                 # 查看详情
greyfall ship-build <设计> <星系编号>        # 建造并编成舰队
greyfall equip <模块> <舰队编号>             # 装配模块
```

模块有槽位限制和船体等级要求：

```bash
greyfall buildings     # 查看建筑与巨构表
```

### 14.3 舰队命令

```bash
greyfall fleets                      # 列出所有舰队
greyfall fleets --id 2               # 查看某支舰队
greyfall fleet --id 2 --order <命令> [--to <星系>]
```

| 命令 | 含义 |
|---|---|
| `move` | 移动到目标星系（需 `--to`） |
| `patrol` | 巡逻 |
| `embargo` | 封锁 |
| `engage` | 交战 |
| `escort` | 护航 |
| `blockade` | 阻断 |
| `retreat` | 撤退 |

舰队有战力、士气、补给三项状态。

### 14.4 法令

```bash
greyfall edict              # 列出所有可用法令
greyfall edict <法令名>      # 颁布
```

法令分两类：

**国家法令**持续 8 季，同类效果不能叠加或提前续期，最多同时维持 3 项。启动一般花费 5,000 cr；军费扩张为 15,000 cr。另有每季维护，无力维持时撤销，到期修正消失。

| 法令 | 生效期间的效果 | 每季维护 |
|---|---|---:|
| 战时配给 | 日常实物需求 −10%，民怨目标 +5% | 500 cr |
| 言论管制 | 情报防御 +10%，稳定目标 +5%，民怨目标 +3% | 600 cr |
| 军费扩张 | 军事修正 +15%，贸易毛利 −5% | 1,500 cr |
| 研究总动员 | 研究速率 +20%，民怨目标 +4% | 1,000 cr |
| 边境戒严 | 稳定目标 +8%，民怨目标 −5%，贸易毛利 −10% | 800 cr |
| 市场自由化 | 贸易毛利 +12%，民怨目标 +4% | 400 cr |

民怨和稳定度向目标逐季收敛；颁布法令不会立即把现有民意刷满。配给不减少工程施工补给。

**满足派系诉求**：`满足军部`、`满足商会`、`满足技术官僚`、`满足原教旨`、`满足劳工`、`满足旧贵族`、`满足民粹`、`满足辛迪加`。

支出为 `(6000 + 派系影响力 × 8000) × (1 + 非首都星系数 × 10%)` cr，影响力按 0～1 取值。满意度提升 25 个百分点、诉求压力清零，同一派系两次援助至少间隔 8 季；不会直接降低全国民怨。通过 `choose` 满足诉求也遵守相同费用和间隔。

### 14.5 研究

```bash
greyfall tech                       # 查看当前立项与已完成
greyfall research <分支>             # 立项到该分支最低层级科技
greyfall research <科技名>           # 立项到指定科技
greyfall research status            # 进度、已投入季数、预计完成
```

六个分支：物理、社会、工程、生物、计算、灵能。

**研究是需要时间的长期项目**：立项后每季推进，任何科技都有最短工期
（1 级 6 季 → 5 级 14 季），资金只能加速、不能跳过。详见第 19 节。

### 14.6 查看增益与减益

```bash
greyfall effects                  # 全部生效中的修正：属性、数值、来源、说明
```

它把分散在各系统的修正集中到一处：

- **属性修正合计**（研究速率、建造速率、军事力量、稳定度……）及其来源
- **生效中的改革**与剩余季数
- **政策**五组的当前选择与过渡状态
- **战争疲劳减益**（若有）：稳定度/民怨/满意度的具体扣减与消除方法
- **政体与种族特质**

每条都附带说明，便于判断该不该继续当前路线。

### 14.7 建造

```bash
greyfall buildings                   # 查看全部建筑
greyfall buildings --planet 7        # 查看某行星已建成的
greyfall build <建筑> <行星编号>      # 建造
```

建造需要拥有该行星、满足科技前置，并付清信用点和全部资源成本。
每个行星同时最多 2 项建筑在建；标记为唯一的建筑每行星限一座，在建项目也占用这个名额。

### 14.8 巨构

```bash
greyfall mega <巨构> <星系编号>
```

每种巨构每国限一座，每个星系限一座，同时只能施工一个巨构阶段。需要该巨构的前置科技；每阶段启动时支付总资金与各项物资的固定分摊额和对应 AP，全部阶段的启动支出之和恰好等于建筑表总价。

从第 1 阶段起，基础工期依次为 8、12、16、20、24 季，再按建造速率缩短，速度最多按 2 倍、每阶段至少 6 季。每季施工维护为总价的千分之一（至少 500 cr），另需能源 30、部件 5。完成一阶段后，再次执行原命令在**同一星系**启动下一阶段。

只有全部完工才有资源、科研、情报或防御收益。建成后每季维护为总资金成本的五百分之一；星系被占领后，收益和维护归新所有者。AI 能看到未完成巨构及后续施工补给需求。

### 14.9 飞升

```bash
greyfall ascend              # 列出可选的飞升路径与代价
greyfall ascend <路径>
```

每条路径每纪元只能完成一次，总计最多两条，同时只能进行一项。启动消耗 3 AP；完工才获得永久效果。

| 路径 | 科技前置 | 启动资金 / 凝聚力 | 工期 | 完工效果 |
|---|---|---|---:|---|
| 心灵 | 灵能 4 项 | 50,000 cr / 2,000 | 12 季 | 情报防御 +15% |
| 机械 | 工程 4 项 | 50,000 cr / 2,000 | 12 季 | 建造速率 +25%，人口增长 −10% |
| 基因 | 生物 4 项 | 50,000 cr / 2,000 | 12 季 | 人口增长 +30%，稳定目标 +10% |
| 虚空 | 物理 4 项 | 50,000 cr / 2,500 | 12 季 | 殖民启动资金修正 −40%，稳定目标 −15% |
| 超脱 | 灵能 12 项 | 120,000 cr / 4,000 | 24 季 | 推进超脱结局维度，开启隐藏幕 |

所有路径启动另需数据晶体 200、部件 150；施工每季消耗 1,000 cr、能源 20、数据晶体 3。机械飞升的修正不会反复乘算库存或基础产能；虚空仍受殖民资金最低 50% 费用倍率约束。

### 14.10 招募

```bash
greyfall recruit
```

招募消耗 1 AP，训练 3 季，同时限一批。报名对象为启动时在本国境内、未参战且士气或补给不足的本国舰队。没有合适对象时不能付费招募。

有 N 支舰队报名时，启动消耗 `3000 + 2000 × N` cr、食物 `30 × N`、药品 `10 × N`、部件 `20 × N`；训练每季维护 `500 + 100 × N` cr、食物 `5 × N`。完工时仅对仍属本国、留在本国境内且未参战的报名舰队补充士气 10%、补给 15%，最高为 100%。招募不会永久叠加反间谍属性。

---

## 15. 机制：舰队与战斗

### 15.1 舰队的五个战斗属性

```bash
greyfall fleets            # 列表：战力 / 组织度 / 老练度 / 士气 / 补给
greyfall fleets --id 0     # 详情：含指挥官、当前战斗、战斗准备度
```

| 属性 | 含义 |
|---|---|
| 战力 | 兵力。决定伤害承受与输出 |
| **组织度** | 战斗的核心。**归零即退出战斗**；只有非交战时才快速恢复 |
| 士气 / 补给 | 影响战力系数。补给不足会加速组织度流失 |
| 老练度 | 由经验累积：新兵 → 老兵 → 精锐 → 王牌 → 传奇（最高 +45% 战力） |
| 战斗准备度 | 静止时累积，进攻时消耗 |

**组织度是理解战斗的关键**：兵力高的舰队不一定赢，组织度先崩的一方撤退。
所以持续作战需要轮换 —— 打残的舰队必须撤到后方休整。

### 15.2 查看战斗

```bash
greyfall battles                 # 全部战斗
greyfall battles --empire 3      # 只看某方参与的战斗
```

输出包含：双方投入舰队数、**展开宽度**、损失、组织度损失、**推进度**（进攻方夺取星系的进度条）、
以及结束原因。

### 15.3 战斗如何发生与结束

**发生**：双方舰队同处一个星系即触发遭遇战。**星系所有者恒为防守方**，另一方是入侵者。

**结束**（按优先级）：
1. 一方组织度归零 → 撤退
2. 一方舰队全部撤出或被歼 → 另一方接管
3. 进攻方推进度满 → 夺取星系
4. 超过 16 季且推进度 < 20% → 进攻方补给不支撤退

**战斗宽度**：同时能展开的战力量级为 1,200–1,900。超出的部分**无法参战**（不会被削弱，但也帮不上忙）。
因此多路进攻优于单点堆叠。

**地形减伤**：首都防御圈 +20%、巨构 +25%、星云 +12%、小行星带 +8%、异常空域 +10%。

### 15.4 指挥官

```bash
greyfall commanders                      # 列表
greyfall commanders --recruit            # 招募（8000 cr + 1 AP）
greyfall commanders --recruit --trait 攻势
greyfall commanders --assign 2:0         # 把指挥官 #2 分配给舰队 #0
```

| 特质 | 效果 |
|---|---|
| 攻势 | 进攻 +15%，防御 -5% |
| 防御 | 防御 +18%，进攻 -5% |
| 后勤 | 补给消耗 -25%，防御 +4% |
| 机动 | 进攻 +8%，展开宽度 +8% |
| 诡道 | 进攻 +6%，压缩对方展开宽度 15% |
| 鼓舞 | 组织度恢复加快，攻防 +5~6% |
| 攻坚 | 进攻 +10%，对要塞/巨构额外有效 |

指挥官有四项技能（攻击 / 防御 / 后勤 / 谋划），随交战累积**经验**与**战绩**。
一个指挥官同时只能带一支舰队。开局每个阵营自动获得 2–3 名并绑定到舰队。

### 15.5 舰队命令与交战

```bash
greyfall fleet --id 2 --order move --to 15      # 开往敌方星系（抵达即触发战斗）
greyfall fleet --id 2 --order engage            # 交战
greyfall fleet --id 2 --order retreat           # 撤退
```

**进攻性命令（engage / blockade / embargo）要求组织度 ≥ 50%**，否则会返回退出码 4 提示先休整。

---

## 16. 机制：将领晋升

### 16.1 军衔

指挥官的军衔由**战功**累积自动晋升，无需手动操作。

| 军衔 | 所需战功 | 技能上限 | 统率加成 | 特质数 |
|---|---|---|---|---|
| 上尉 | 0 | 70% | +0% | 1 |
| 少校 | 120 | 77% | +4% | 1 |
| 上校 | 360 | 84% | +8% | 2 |
| 少将 | 840 | 91% | +12% | 2 |
| 元帅 | 1,800 | 98% | +16% | 3 |

**统率加成直接扩大战斗展开宽度** —— 高军衔指挥官能同时投入更多战力。

### 16.2 战功从哪来

| 来源 | 获得 |
|---|---|
| 战斗胜利 | 基础 40 + 持续季数 × 6，再 ×1.5 |
| 战斗失败 | 基础 30 + 持续季数 × 5（败方也有收获） |
| 军校深造 | +60（另需花费影响力） |
| 开局 | 老牌帝国的军官起点略高（0~60） |

战斗规模越大、持续越久，战功越高。一次授予大量战功可以**连续跨级晋升**。

### 16.3 特质的累积

每晋升到有更多特质槽的军衔时，自动习得一项**尚未拥有**的特质。
主特质与已习得特质的效果**全部累加**。

例如「机动/攻势/防御」的指挥官同时获得：
机动（进攻 +8%、宽度 +8%）、攻势（进攻 +15%、防御 -5%）、防御（防御 +18%、进攻 -5%）。

### 16.4 军校深造

```bash
greyfall commanders --train 3      # 花费 300 影响力
```

效果：攻击/防御 +3%、后勤/谋划 +2%、战功 +60。

### 16.5 查看

```bash
greyfall commanders
```

输出含军衔、全部特质、四项技能、战绩、战功，以及**距下一级军衔还需多少战功**。

```bash
greyfall commanders --recruit --trait 攻势     # 招募
greyfall commanders --assign 2:0               # 分配到舰队
```

---

## 17. 机制：前线与战线

### 17.1 查看战线

```bash
greyfall military                  # 军备：军力/上限/难度加成/补满工期
greyfall front                  # 全部战线总览
greyfall front --detail 3       # 对某国的战线分析（含敌方纵深薄弱点）
```

**扇区（sector）**是战线的单位：每个扇区是一个**可进攻的敌方星系**。

| 列 | 含义 |
|---|---|
| 目标星系 | 该扇区指向的敌方星系 |
| 出发自 | 我方相邻星系（兵力从这里投入） |
| 地形 | 影响防守方减伤 |
| 我方战力 | 该扇区可用兵力（含相邻我方星系的协同） |
| 敌方守备 | 敌方舰队 + 要塞 + 地形加成 |
| 比 | 我方战力 ÷ 敌方守备 |
| 状态 | `★ 建议主攻` / `⚔ 交战中` |

**姿态**由整体战力比给出：

| 战力比 | 姿态 |
|---|---|
| ≥ 2.0 | 全面优势：可多路推进 |
| ≥ 1.2 | 局部优势：建议单点突破 |
| ≥ 0.8 | 均势：宜固守待援 |
| < 0.8 | 劣势：建议转入防御或求和 |

### 17.2 多路突破

当整体战力比足够高时，系统会**同时开辟多条进攻轴**，而不是把兵力堆在一个扇区。

| 战力比 | 进攻轴数 |
|---|---|
| < 2.0 | 1（集中兵力，单点突破） |
| ≥ 2.0 | 2 |
| ≥ 3.0 | 3 |
| ≥ 5.0 | 4 |

每条轴独立计算**达成突破所需的战力**（敌方守备 × 1.2），系统按轴依次补兵，
并保留 20% 作为预备队。`front` 会列出当前方案：

```
多路突破：2 条进攻轴同时推进
  · 奥密脊-9     需战力     494  现有     945  可独立突破
  · 泽塔锚-41    需战力     669  现有     832  可独立突破
```

优势不足时显示 `单点主攻（优势不足以分兵）` —— 此时分兵只会让每路都不够强。

### 17.3 自动部署

每季系统会把**闲置舰队**（命令为「待命」或「巡逻」）自动派往兵力缺口最大的扇区。

**投入量按整体态势决定**：

| 整体战力比 | 投入比例 |
|---|---|
| ≥ 1.2（优势） | 全部可分配舰队 |
| 0.8 ~ 1.2（均势） | 2/3 |
| < 0.8（劣势） | 1/3（保留战略预备队） |

想让某支舰队固定在原地、不参与自动分配：把它设为 `blockade` / `escort` / `retreat` 即可。

手动指定方向：

```bash
greyfall fleet --id 2 --order move --to 15     # 开往指定星系
greyfall fleet --id 2 --order engage           # 就地交战
```

### 17.4 接触扇区

若敌方舰队已经进入**你的**星系（或你的舰队已进入敌方星系），该星系会作为**接触扇区**出现在战线上 ——
即使它与你的其他领土并不接壤。这样你在被入侵时不会「看不到战线」。

---

## 18. 机制：议会立法

### 18.1 与「政策」的区别

| | `policy`（政策） | `parliament`（法案） |
|---|---|---|
| 决定权 | 玩家单方面推行 | **必须议会表决通过** |
| 成本 | 影响力 + 每季维护 | 影响力 + 政治资本 + 拉票开销 |
| 生效 | 过渡期后自动生效 | 表决通过后**立即永久生效** |
| 阻力 | 无 | 派系立场与席位分布 |

### 18.2 议会席位

8 个派系按**影响力**分配席位（每派系至少 1 席）。影响力会随局势变化，席位每季重算。

```bash
greyfall parliament            # 议会总览 + 全部 36 项法案
```

**通过门槛由政体决定**：

| 政体 | 门槛 |
|---|---|
| 民主制 / 共和制 | 简单多数（>50%） |
| 寡头制 / 独裁制 / 神权制 / 军事委员会 | 绝对多数（≥60%） |
| 蜂群意识 / 无政府 | 特别多数（≥2/3） |

### 18.3 立法流程

```bash
greyfall parliament --detail 土地改革      # 1. 查看法案：效果、成本、各派系天然立场
greyfall parliament --propose 土地改革     # 2. 提交，查看各派系当前立场
greyfall parliament --persuade 军部        # 3. 拉票（可多次，逐个派系）
greyfall parliament --vote                 # 4. 表决
```

**拉票**：把某派系的立场提升一格（反对 → 未定 → 赞成）。代价是政治资本 + 影响力 + 国库。
**政治资本**每季恢复 2%，上限 100%。

**未定派系**按满意度倾向**比例**投赞成/反对 —— 满意者倾向支持温和法案，不满者倾向支持激进改革。

**强行通过**：

```bash
greyfall parliament --force      # 不需要票数，但代价极高
```

代价：政治资本 -35%、民怨 +12%、合法性 -15%、反对派满意度 -20%。

### 18.4 法案标记与效果

法案名后的 **⚡** 表示**激进改革**：保守派强烈反对，但不满者更愿支持。

已通过的法案**永久生效**，效果直接进入国力计算（`建造速率 +20%` 等）。
它们同时持续影响派系关系：支持者满意度缓慢上升，反对者缓慢下降。

### 18.5 AI 也会立法

非玩家帝国同样会提案、拉票与表决，选案依据是**伦理与处境**
（尚武国推军事法案、商贸国推自由贸易、民怨高时优先安抚类法案）。
可查看各帝国的立法记录，了解其当前制度。

---

## 19. 机制：科技与政策

### 19.1 科技树

6 个分支 × 16 项 = 96 项科技。

```bash
greyfall tech                  # 全貌：当前立项、进度、专精、已完成
greyfall tech --branch 工程     # 某分支的逐项列表（含效果与状态）
greyfall research 工程          # 立项，之后逐季推进
greyfall research status       # 当前项目进度与预计完成季数
```

| 分支 | 主轴 | 次轴 |
|---|---|---|
| 物理 | 研究速率 | 侦测 |
| 社会 | 稳定 | 影响力增益 |
| 工程 | 建造速率 | 贸易毛利 |
| 生物 | 人口增长 | 民怨（降低） |
| 计算 | 研究速率 | 操纵技巧 |
| 灵能 | 情报防御 | 外交权重 |

**每项科技都提供 2 条属性修正**，量级随层级递增：tier1 约 +2%，tier5 约 +10%。
`greyfall tech --branch <分支>` 会逐项列出效果。

**分支专精**：某分支每完成 4 项，该分支主轴额外 +3%（4/8/12/16 各一档）。
**满级奖励**：某分支 16/16 全部完成，主轴再 +10%。

**研究侧重**：每个帝国在开局时按伦理与立场确定侧重。
AI 在可研究的最低层级中优先选择侧重分支；玩家的攻关目标由 `research` 指定。

**投入方式**：

```bash
greyfall research 工程            # 立项：把工程分支最低层级的科技设为攻关目标
greyfall research comp6           # 立项到指定科技
greyfall research 工程 --fund 15000   # 立项并设定每季投入 15000 cr
greyfall research status          # 查看当前项目的进度与预计完成季数
```

**研究需要时间。** 立项后每季推进一次，满足以下两点才完成：

| 条件 | 说明 |
|---|---|
| 累计研究点 ≥ 成本 | 由「基础研究速率 + 每季资金投入」累积 |
| 已投入季数 ≥ 最短工期 | **钱再多也不能更快** |

**最短工期**随层级递增：

| 层级 | 1 | 2 | 3 | 4 | 5 |
|---|---|---|---|---|---|
| 最短工期 | 6 季 | 8 季 | 10 季 | 12 季 | 14 季 |

每季推进量还有一个上限：`成本 ÷ 最短工期`。所以哪怕国库堆积如山，
一项科技也至少要花满最短工期 —— **时间买不到，只能等**。

**每季投入**（`--fund`，默认 8000 cr）决定推进速度，由 `advance` 逐季从国库扣除；
国库见底时会自动降档，不会透支。

**同时只能攻关一项**：立项新科技会丢弃旧项目的进度。

建筑与科学家提高当季研究效率。间谍、道具或事件获得的研究资料会先储存，
在对应分支有项目时逐季消耗，同样受每季推进上限和最短工期约束。

**跨分支前置**：少数关键节点需要另一分支的成果（例如社会·心理画像需要计算·量子计算）。
`greyfall tech --branch <分支>` 可看到「锁定」状态与所需前置。

### 19.2 政策

政策是**持久化**的法令，与持续 8 季的国家 `edict` 互补。

```bash
greyfall policy                    # 全部五组政策与当前状态
greyfall policy --detail 战时统制    # 单条详情：成本、维护、过渡期、效果、派系反应
greyfall policy --enact 战时统制     # 推行
```

| 组 | 可选政策 |
|---|---|
| 经济体制 | 自由市场 / 国家主导 / 战时统制 / 生态经济 |
| 军事体制 | 志愿兵役 / 义务兵役 / 要塞主义 / 雇佣军团 |
| 社会体制 | 开放社会 / 管制社会 / 福利国家 / 精英择优 |
| 外交路线 | 开放外交 / 孤立路线 / 霸权路线 / 藩属网络 |
| 情报体制 | 透明情报 / 秘密机构 / 灵能军团 |

**规则**：

- **同组互斥**：每组同时只有一项生效。
- **推行成本**：一次性消耗影响力（0–500）。
- **过渡期**：1–3 季，期间新旧效果**线性切换**（不是瞬间跳变）。
- **维护费**：按季扣除（0–900 cr/季），在 `greyfall policy` 底部显示合计。
- **派系反应**：每项政策有受益方与受损方。例如「战时统制」使军部 +16%、劳工 -16%。
  该影响会持续作用（每季再微调），因此长期政策会重塑国内政治格局。

---

## 20. 机制：决议系统

决议是一次性的政策决策，共 48 项，分五类。查看方式：

```bash
greyfall resolve                # 全部决议 + 生效中的效果 + 进行中的倒计时
greyfall resolve --available    # 只列出当前可主动发起的
greyfall resolve --detail <id>  # 查看单项详情（条件、代价、效果）
greyfall resolve --activate <id>  # 发起（仅对「主动决议」与「牺牲换利」）
```

### 最重要的一条规则

**改革同时只能推行一项。**

主动决议与「牺牲换利」类占用同一个改革槽位。要推行新的，必须先等当前这项
走完 24 季（或倒计时型议程结束）。因此「先改什么、什么时候改」是真正的战略决策 ——
不能把所有有利效果一次性叠满。

> 倒计时型（`--detail` 里标注为「倒计时」）是**议程**而非改革方向，
> 不占这个槽位；它们占用的是时间与失败风险。

### 五类决议

| 类型 | 触发方式 | 说明 |
|---|---|---|
| **主动决议** | 你执行 `--activate` | 支付代价，立即获得收益，**持续 24 季** |
| **自动触发** | 条件满足自动发生 | 可能是增益也可能是减益，不需要你做任何事 |
| **可阻止** | 条件不满足时自动触发减益 | 满足「阻止条件」即可完全避免 |
| **倒计时** | 条件满足时启动 | 在期限内达成目标 → 永久增益；超时 → 惩罚（持续等同期限） |
| **牺牲换利** | 你执行 `--activate` | 永久牺牲一项属性，换取另一项永久提升 |

### 主动决议示例

```bash
greyfall resolve --detail 科研拨款
greyfall resolve --activate 科研拨款
```

执行 `greyfall resolve --detail 科研拨款` 查看所需信用点、行动点、研究加成与持续时间，满足条件后再发起。

### 可阻止决议示例

`货币危机`：第 12 季后，若国库不足 30000，自动触发「信用评级 -20%、贸易毛利 -15%」，各持续 6 季。
**阻止条件：国库 ≥ 30000。** 满足即完全避免。

其他可阻止项：`分离主义`（民怨 ≤ 40% 阻止）、`军部异动`（稳定度 ≥ 45% 阻止）、`瘟疫`（药品 ≥ 1500 阻止）。

### 倒计时决议示例

```bash
greyfall resolve --detail 基建攻坚
```

`基建攻坚`：第 10 季后启动，8 季内把稳定度提到 65%。
- 达成 → 稳定度相关永久增益
- 超时 → 国库损失（惩罚持续 8 季）

同类：`扩军计划`（军力 ≥ 1500）、`民族和解`（民怨 ≤ 25%）、`贸易峰会`（贸易毛利修正 ≥ +10%）。

### 牺牲换利示例

```bash
greyfall resolve --detail 劳动征召
greyfall resolve --activate 劳动征召
```

`劳动征召`：建造速率**永久** +25%，人口增长**永久** -15%。

其他：`战时经济`（军力 +30%，贸易毛利 -18%）、`孤立主义`（稳定度 +20%，外交权重 -25%）、
`紧缩财政`（信用 +20%，稳定度 -10%）、`秘密警察`（侦测 +30%，民怨 +15%）。

### 生效中的效果

`greyfall resolve` 会列出所有正在生效的效果及其剩余季数。
「永久」表示该效果不会过期（通常来自牺牲换利或倒计时成功奖励）。

---

## 21. 机制：决议的互斥与依赖

### 21.1 互斥组

48 项决议分属 **6 个互斥组**，同组只能有一项生效。

| 组 | 成员 |
|---|---|
| 经济路线 | 战时经济 / 紧缩财政 / 平准基金 / 科研拨款 |
| 社会控制 | 秘密警察 / 公民庆典 / 公共卫生运动 / 国族叙事 |
| 对外姿态 | 孤立主义 / 大阅兵 / 边境筑垒 |
| 劳动与人力 | 劳动征召 / 青年服役 / 老兵抚恤 |
| 知识投入 | 学院捐赠 / 档案工程 / 文化交流 |
| 财政手段 | 战时公债 / 税收赦免 |

若已有同组决议生效，再发起会被拒绝并说明冲突对象：

```
greyfall resolve --activate 科研拨款
→ 与生效中的【紧缩财政】互斥（同属「经济路线」）
```

### 21.2 依赖链

部分决议需要先完成前置决议。

| 决议 | 前置 |
|---|---|
| 战时经济 | 战时公债 |
| 劳动征召 | 战时经济 |
| 国族叙事 | 公民庆典 |
| 大阅兵 | 边境筑垒 |
| 孤立主义 | 边境筑垒 |
| 秘密警察 | 边境筑垒 |
| 公共卫生运动 | 城市更新 |
| 平准基金 | 航道开辟 |

```
greyfall resolve --activate 劳动征召
→ 需要先完成【战时经济】
```

### 21.3 阻止关系

某些决议生效期间会**阻止**其他组的决议。

| 生效中的决议 | 被阻止 |
|---|---|
| 秘密警察 | 文化交流 |
| 孤立主义 | 文化交流 |
| 战时统制 | 公共卫生运动 |
| 紧缩财政 | 公共卫生运动、城市更新 |

### 21.4 查看完整约束

```bash
greyfall resolve --detail 劳动征召
```

输出会列出：**互斥组及其全部成员**、**前置决议及各自的完成状态（✓/✗）**、
**被本决议阻止的目标**，以及**当前可否发起**（不可时给出具体原因）。

约束同时作用于**玩家发起**与**自动触发** —— 被阻止的决议不会自行出现。

---

## 22. 机制：谈判与提案

### 22.0 AI 会主动找你谈

AI 会定期提出**提案**，进入你的提案箱：

```bash
greyfall proposal                  # 列出待回应的提议
greyfall proposal --detail 41      # 看细节：内容、接受/拒绝的后果、公平度
greyfall proposal --accept 41      # 接受
greyfall proposal --reject 41      # 拒绝（对方观感 -6%）
```

| 提案类型 | 内容 |
|---|---|
| 资源互换 | 按市价交换商品 |
| 研究协定 | 双方研究速率 +0.15（40 季） |
| 互不侵犯 | 60 季内不宣战 |
| 联合情报 | 双方反间谍 +15% |
| 领土互换 | 接壤星系对等交换（罕见） |
| 索贡 | 对方以实力压你，付钱免战 |

每份提案都标注**公平度**：正数对你有利，负数对你不利。

**你可以谴责任何提案**——包括合理的：

```bash
greyfall envoy 3 condemn
```

代价：对方观感 -12%、你的信誉 -8%。收益：军部与民粹满意 +5%，商会与劳工 -4%。
这是政治姿态，不是纯粹的得失计算。

### 22.1 领土交易：权重最低

领土在 AI 的偏好里**权重最低**，规则很硬：

| 规则 | 说明 |
|---|---|
| **必须互换** | 单方面索取领土一律拒绝。确要用武力拿地，走和平会议 |
| **必须接壤** | 你给出的星系要与对方领土相邻；索取的星系要与你相邻。飞地交换不受理 |
| **首都不参与** | 任何一方首都都不可交易 |
| **主权溢价** | 即使合法互换，也要额外补偿约 2.2 倍价值——割地有政治代价 |

星系价值按**真实内容**计算：行星数量、人口、开发度、巨构与首都溢价。

### 22.2 三档评价

谈判结果会给出提案对**对方**的公平度：

| 档次 | 判定 |
|---|---|
| **合理** | 对方不亏 |
| **稍微不合理** | 对方小亏（不超过索取价值的 25%） |
| **很不合理** | 对方明显吃亏 |

无论成败都会给出评价——这是你决定要不要谴责的依据。

### 22.3 AI 之间也会交易

AI 之间每 4 季结算一次双边贸易，动机是**库存差异**（我积压、你紧缺）。
成交会改善双方关系，形成"越贸易越亲近"的正反馈。

---

## 23. 机制：谈判条款

谈判不是随时可用的。**AI 不会主动倾向于谈判**——只有当「谈判权重」累积到阈值以上时，它才肯坐上谈判桌。

```bash
greyfall negotiate <emp> --status                  # 查看权重分解
greyfall negotiate <emp> --demand "credits=20000"  # 单方面索取
greyfall negotiate <emp> --demand "..." --offer "..."  # 等价交换
```

### 谈判权重

权重由五项压力累积而成：

| 来源 | 何时升高 |
|---|---|
| 领土压力 | 对方星系数远超自己 |
| 经济压力 | 对方经济体量远超自己 |
| 制裁压力 | 自己正被封锁或制裁 |
| 战争压力 | 处于战争且战果落后 |
| 军事劣势 | 自己的军力远逊于对方 |

额外加成：**首都已被占领 +60%**，**已放弃抵抗 +25%**。

阈值默认 35%；若对方判定你是「剥削型」则 +15%（更不愿谈），你主动上门再 +5%（对方抬价）。

`--status` 会完整列出每一项的百分比与阈值。

### 条款格式

```
credits=<数量>      信用点
influence=<数量>    影响力
unity=<数量>        凝聚力
manpower=<数量>     人力（千人）
tech=<科技名>       科技，如 tech=comp6
system=<星系编号>   星系，如 system=12
<资源名>=<数量>     任意商品，如 alloys=5000
```

多个条款用逗号分隔。`--demand` 是要求对方付出，`--offer` 是你愿意付出。

### 示例

```bash
# 索取资金与资源
greyfall negotiate 3 --demand "credits=20000,alloys=5000"

# 用钱换科技
greyfall negotiate 3 --demand "tech=comp6" --offer "credits=50000"

# 索要领土
greyfall negotiate 3 --demand "system=12" --offer "alloys=8000"

# 索要人力
greyfall negotiate 3 --demand "manpower=300" --offer "credits=15000"
```

### 接受与拒绝

- **权重未达阈值** → 直接拒绝，谈判不成立。
- **权重达标但条件不对等** → 拒绝，并给出「需要多少价值」的提示。
  （对方会要求一个「威慑溢价」，权重越低溢价越高。）
- **权重达标且条件对等** → 接受并立即执行条款。

### 首都占领 = 无条件接受

若对方**首都被你占领**且**已放弃抵抗**（舰队全灭或军力不足你的 25%），
则对方**必须无条件接受你的任何条款**，无论多么苛刻。

```bash
greyfall negotiate 3 --status    # 会显示「首都状态」与「抵抗意志」
```

此时 `--demand "credits=999999,alloys=999999"` 也会被接受（实际只能拿走对方拥有的部分，
且保留最低限度：国库最多拿 90%、人口最多迁走 60%、星系至少留 1 个）。

### 盟友代付

若对方无力支付你索要的条款，其**盟友**（同联邦成员或防御同盟）会按各自的可支付额度**代付**。
义务因此转移到盟友身上——被榨干的可能是原本中立的第三方。

```bash
greyfall negotiate 3 --status    # 底部会列出对方所有盟友及其国库/影响力/星系
```

谈判成功后，输出中会明确列出哪些条款由哪个盟友代付。

---

## 24. 机制：战争理由与战争疲劳

### 24.1 宣战需要正当理由

**不能随时对任何国家开战。** 必须先持有 casus belli：

```bash
greyfall casus                    # 查看持有的全部理由 + 各场战争的疲劳度
greyfall envoy 3 fabricate        # 伪造宣称（400 影响力，30 季后失效）
greyfall envoy 3 declare-war      # 有理由才能宣战
```

| 理由 | 来源 | 有效期 |
|---|---|---|
| **领土争端** | 对方持有与你接壤的星系（每 5 季自动评估） | 长期 |
| **反制制裁** | 对方正在制裁或禁运你 | 长期 |
| **盟友受侵** | 你的防御同盟成员正与对方交战 | 长期 |
| **边界摩擦** | 接壤时每 5 季 8% 概率触发 | 20 季 |
| **伪造宣称** | 花 400 影响力制造 | 30 季 |

**伪造的代价**：目标观感 -15%，全体第三方观感 -4%。它经不起检验。

**没有理由强行开战**：加 `--no-casus`，代价是全体第三方观感 -20%、稳定度 -8%、
商会与劳工满意度 -8%。这是政治豪赌，不是免费选项。

### 24.2 战争疲劳与反战

战争会累积**疲劳**，按对手分别计算：

| 疲劳 | 后果 |
|---|---|
| < 50% | 无影响 |
| ≥ 50% | 稳定度最高 -15%、民怨最高 +12%（随疲劳线性增长） |
| ≥ 80% | 派系**公开要求停战**；继续作战则满意度 -18% |
| 停战后 | **疲劳与全部减益一并消除** |

疲劳增速约 2~4%/季：基础 2%，战况不利（战果为负）+1%，
民主政体对选民更敏感，再 +1%。

**求和更容易**：对方的疲劳会提高接受停火的概率（最高 +40%）。

```bash
greyfall casus                    # 看各场战争的疲劳与当前减益
greyfall envoy 3 plead-peace      # 求和；成功即清除疲劳
```

---

## 25. 机制：战争迷雾与情报

### 25.1 你看不到精确数字

`greyfall empires` 列出的他国**国库、国力、舰队数、观感**都是**区间**：

```
 1  伽兰帝国  约 246.52 ~ 701.64   约 31,371 ~ 153,163   约 1 ~ 5  存活
```

因为你没有情报。想知道确切数字，必须先建立情报来源。

### 25.2 情报门槛

每类信息有不同的**情报等级**要求：

| 信息 | 需要 | 说明 |
|---|---|---|
| 军力 | 20% | 最容易看出——边境就能观察 |
| 舰队位置 | 30% | |
| 国库 | 40% | |
| 外交关系 | 45% | |
| 科技进度 | 55% | |
| 政策与决议 | 65% | 最难掌握 |

未达门槛时只给**区间**，误差随情报缺口放大（±30% ~ ±90%）。

### 25.3 情报来源

| 来源 | 贡献 |
|---|---|
| 间谍网络渗透 | 渗透度 × 70%（最主要） |
| 联合情报条约 | +22% |
| 研究协定 | +12% |
| 防御同盟 | +10% |
| 互不侵犯 | +5% |
| 贸易路线 | +6% |
| 接壤 | +8% |
| 交战状态 | +15% |

**对方的反间谍与情报防御会扣减**：反间谍每 1% 扣 0.4%，情报防御每 1% 扣 0.25%。

### 25.4 反间谍：让迷雾回来

对方加强反间谍时，你的情报等级会暴跌：

```
渗透完成、无额外反间谍：情报等级 84%   → 国库/科技/政策全部可见
对方反间谍 80% + 情报防御 90%：34%     → 全部重新变为模糊
```

反间谍会**自然衰减**（每 8 季 ×98%），所以一次投入不能永久遮蔽——
情报战是持续的拉锯，不是一劳永逸。

### 25.5 查看情报图景

```bash
greyfall intel --empire 3 --fog      # 情报图景 + 来源分解
greyfall network --establish 3       # 建立间谍网络提高情报
```

`--fog` 会列出每类信息的**等级 / 门槛 / 状态 / 你看到的**，
以及每一项来源的具体贡献——便于判断该往哪个方向补情报。

---

## 26. 机制：人事（领袖 / 科学家 / 集团军）

```bash
greyfall personnel                # 总览：领袖、科研部、集团军
```

### 26.1 领袖

每位领袖有**出身**，提供全国性修正，并随技能放大：

| 出身 | 效果 |
|---|---|
| 行政干才 | 建造速率 +10%、稳定度 +8% |
| 军事强人 | 军事力量 +12% |
| 学者出身 | 研究速率 +15% |
| 商界背景 | 贸易毛利 +10%、信用评级 +10% |
| 煽动家 | 影响力增益 +15% |
| 改革者 | 稳定度 +6%、政策过渡更快 |

**技能**以 50% 为基准，每高 10% 让主修正多 20%。

**更替**：实行选举的政体在任期到期后进入 6 季竞选，结果确定时才交接。
新任期从选举结果公布时起计算 40 季；竞选期间在位者继续执政。
非选举政体的领袖通常终身任职。领袖每 10 季增长 1 岁，年过 70 后每季有 6% 概率过世。
选举换人降低 5 个百分点稳定度，连任提高 6 个百分点合法性；领袖过世降低 6 个百分点稳定度。

### 26.2 科研部科学家

```bash
greyfall personnel --recruit                     # 招募（150 影响力，最多 6 位）
greyfall personnel --assign 2 --branch 2         # 派往工程分支
```

每位科学家有**专长分支**与**能力**。派往某分支后，该分支的立项推进加速：

- 基础加成 = 能力 × 40%
- **专长对口**额外 +15%

科学家加成也受每季研究上限约束，**不能突破最短工期**；1 级科技至少需要 6 季。

### 26.3 编队与集团军

舰队可以编成**集团军**，由**集团军司令**统率：

```bash
greyfall personnel --form 先锋集群               # 新建
greyfall personnel --add 1 --fleet 0             # 编入舰队
greyfall personnel --lead 1 --commander 0        # 任命司令
greyfall personnel --remove 1 --fleet 0          # 移出
greyfall personnel --disband 1                   # 解散
```

**战力倍率**来自三部分：

| 来源 | 加成 |
|---|---|
| 协同度 | 最高 +15%（多支舰队同处一星系时每季 +3%，分散时 -1%） |
| 司令 | 指挥官攻防与谋划之和 ÷ 12 |
| 编制规模 | 3 支以上 +5%（1 支形同虚设） |

一支舰队只能属于一个集团军；编入新集团军会自动从旧的移出。
**未编入的舰队没有任何加成**——单打独斗是纯损失。

---

## 27. 机制：政体差异

```bash
greyfall gov                     # 政体详情 + 合法性来源 + 选举状态
greyfall gov --elect             # 提前改选（仅选举制）
greyfall gov --support 1         # 公开支持候选人 #1（200 影响力）
greyfall gov --suppress          # 镇压（仅威权制）
```

### 27.1 合法性来源：政体的核心区别

18 种政体按**合法性如何获得**分为六类，这决定了它们的玩法：

| 来源 | 政体 | 合法性如何涨落 |
|---|---|---|
| **选举授权** | 民主制 / 共和制 / 赛博共和 / 民主主义 | 民怨打击最大（-35%）；完整竞选结束后更替领袖 |
| **血统传统** | 帝制 / 部落议会 | 稳定但僵化；领袖过世可能触发**继承危机**（-25%） |
| **恐惧镇压** | 寡头制 / 独裁制 / 军事委员会 | 可**主动镇压**（+25%），但怨恨反噬（-20%） |
| **绩效表现** | 技术官僚制 / 企业制 / 社会主义 / 法西斯主义 / 资本主义 | 看国库与战果：盈余 +10%，赤字 -12% |
| **信仰教义** | 神权制 / 神谕制 | 对经济不敏感，异见仍侵蚀（-15%） |
| **共识协同** | 蜂群意识 / 无政府 | 几乎不受短期波动影响，但决策迟缓 |

### 27.2 民主制：选举

**任期 40 季，到期进入 6 季竞选**（也可 `--elect` 提前启动）。
选举结果公布后开始计算下一任期；竞选期间不会因旧任期已到期而自动换人。共有 3 位候选人：

```
 #  候选人  出身      能力  支持率  备注
 0  法华    改革者    55%   36%    在位者
 1  柯诺    军事强人  62%   41%
 2  恩奇    改革者    48%   23%
```

- **在位者支持率受民怨影响**：民怨越高，在位者越容易被赶下台
- `--support <编号>` 花费 200 影响力，为该候选人 **+12%** 支持率
- **换人**：稳定 -5%；**连任**：合法性 +6%

民主制**不能镇压**（会适得其反）。

### 27.3 独裁制：镇压与怨恨

```bash
greyfall gov --suppress
已实施镇压（合法性 +5%，民怨 -6%，但**怨恨 +12%**）
```

镇压是**双刃剑**：

| 即时效果 | 长期代价 |
|---|---|
| 合法性 +5%、民怨 -6% | 怨恨 +12%，每季把民怨往上推 |

怨恨每季消退 2%，镇压强度每季衰减 1%——**必须持续投入**才能维持高压。
停止镇压后，合法性会回落到基线。

独裁制**不举行选举**，领袖终身制（有过世概率）。

### 27.4 查看当前政体

`greyfall gov` 会列出：政体、合法性来源、行动点加成、合法性基线、
当前合法性与来源提供的合法性、领袖产生方式、可否镇压，
以及（威权制）镇压强度与累积怨恨。

---

## 28. 机制：舰船设计与改造

```bash
greyfall design                                     # 舰体一览 + 你的设计
greyfall design --new destroyer --name 突击型        # 新建自定义设计
greyfall design --install 突击型 --module 3          # 安装模块
greyfall design --remove 突击型 --at 0               # 卸下第 0 槽
greyfall design --clear 突击型                       # 清空模块
greyfall design --refit 0 --to 突击型                # 改造现役舰队
```

### 28.1 六种舰体各有定位

| 舰体 | 定位 | 造价 | 火力 | 防御 | 速度 | 槽位 |
|---|---|---|---|---|---|---|
| 护卫舰 | **屏卫** | 1,200 | 30 | 20 | 40 | 3 |
| 护卫舰-重型 | **隐匿** | 2,600 | 55 | 45 | 34 | 4 |
| 驱逐舰 | **突击** | 5,400 | 95 | 80 | 28 | 5 |
| 巡洋舰 | **战列** | 12,000 | 170 | 150 | 22 | 6 |
| 战列舰 | **攻坚** | 30,000 | 320 | 300 | 16 | 8 |
| 泰坦 | **母舰** | 90,000 | 680 | 620 | 10 | 10 |

- **屏卫**：廉价量大，吸收火力掩护主力
- **突击**：火力突出、防御薄弱，适合集中突击
- **战列**：攻防均衡的编队中坚
- **母舰**：远程投送，自身脆弱需屏卫保护
- **隐匿**：侦察与偷袭，正面弱
- **攻坚**：对恒星基地与巨构特化

### 28.2 模块真正影响战斗

这是关键：**模块的火力与防御直接进入战力计算**。

以「(火力+防御) 相对该舰体基线」的比值作为倍率：

```
原始舰队（4 模块）：      战力 456.6
清空全部模块：            战力 304.4   （-33.3%）
装满武器模块：            战力 627.1   （+37.3%）
```

**裸舰体远弱于装配合格的设计**——不装模块等于白送。

模块受两个限制：
- **舰体等级**：高级模块需要更高舰体（`minHull`）
- **槽位数量**：每个舰体有固定槽位（护卫舰 3 槽、泰坦 10 槽）

### 28.3 改造现役舰队

```bash
greyfall design --refit 0 --to 突击型
【第二舰队】已改造为 突击型（合金 -20，组织度清零需重整训）
```

| 条件 | 说明 |
|---|---|
| 位置 | 必须在**自己的星系**内（需要船坞） |
| 成本 | 目标设计造价的 **45%**（合金 + 信用点） |
| 代价 | 组织度**清零**，需要时间重新整训 |
| 战力 | 按新旧设计的火力比缩放 |

改造让你能把老旧舰队升级到新设计，而不必拆掉重建。

---

## 29. 机制：恒星基地与建筑

### 29.1 恒星基地（星系级）

与**行星建筑**不同：基地建在**星系**上，一座星系一座，是主权的实体标志。

```bash
greyfall base                        # 基地列表 + 升级链
greyfall base --found 25             # 建立前哨站
greyfall base --upgrade 25           # 升级一级
greyfall base --dismantle 25         # 拆除（不返还）
```

| 等级 | 信用点 | 合金 | 防御 | 补给 |
|---|---|---|---|---|
| 前哨站 | 8,000 | 60 | 120 | +10% |
| 星港 | 22,000 | 180 | 400 | +25% |
| 堡垒 | 60,000 | 520 | 1,100 | +45% |
| 要塞 | 150,000 | 1,400 | 2,600 | +70% |
| 星际要塞 | 380,000 | 3,600 | 6,000 | +100% |

建立与升级均需施工，每国同时最多两个基地项目；每座基地不能同时重复升级。前哨站至星际要塞分别需要 4 / 6 / 8 / 10 / 12 季，施工维护分别为 200 / 300 / 400 / 500 / 600 cr 每季，另消耗能源 10 / 15 / 20 / 25 / 30。

基地建成前没有防御或补给加成；升级期间保留旧等级效果。建成后的日常维护分别为 50 / 200 / 450 / 800 / 1,250 cr 每季。通过 `queue` 查看施工或取消；正在升级时须先取消工程才能拆除。

**完工后的防御直接计入该星系的防守战力**，与驻军叠加：

```
无基地：  星系防守 1,021   进攻方胜算 85.9%
要塞：    星系防守 3,621   进攻方胜算 71.3%   ← 下降 14.6 个百分点
```

**星系易主时基地随之易主并降一级**——攻占的工事需要修复。

### 29.2 行星建筑的四类实效

共有 **28 种行星建筑**。除资源生产、驻军与宣传等用途外，以下建筑提供研究、贸易、仓储或线索加成：

| 建筑 | 类别 | 作用 |
|---|---|---|
| 研究实验室 | 研究 | 提高当前研究速率，仍受研究工期约束 |
| 贸易枢纽 / 市场监察署 | 贸易 | 分别增加 15% / 10% 贸易加成，用于路线运力 |
| 黑库 / 仓储环 | 仓储 | 分别提高 2000 / 1200 单位战略物资额外库存维护费的计费门槛 |
| 档案穹顶 / 异常解析站 | 线索发现 | 分别增加 18% / 25% 发现加成，提高异常解析时取得线索的概率 |

上述加成按当前拥有的建筑计算。新建筑完成后生效，失去建筑后相应加成也会消失。
战略物资库存超过 50,000 单位的部分需要支付额外维护费；仓储建筑提高这一门槛，不减少日常资源需求。
线索发现加成是对基础概率的相对提升，例如基础概率 12% 加上 18% 发现加成后约为 14.16%。

---

## 30. 机制：起义与党派斗争

```bash
greyfall revolt                          # 境内动乱 + 派系格局 + 通牒
greyfall revolt --suppress 25            # 镇压某星系的动乱
greyfall revolt --concede                # 满足派系诉求
greyfall revolt --refuse                 # 拒绝最后通牒
```

### 30.1 动乱四阶段

民怨与派系不满会把星系一步步推向脱离：

| 阶段 | 触发风险 | 后果 |
|---|---|---|
| 平静 | < 50% | 正常 |
| **不安** | ≥ 50% | 产出下降、驻军增加（此时安抚成本最低） |
| **叛乱** | ≥ 65% | 停止纳贡，驻军只维持最低秩序 |
| **割据** | ≥ 82% | **持续 8 季未平息即脱离控制** |

**脱离的两种去向**：
- **倒向邻国** —— 接壤且非交战的国家会接收（伦理越近越可能）
- **自立为国** —— 成立「XX自由邦」，与原宗主**立即开战**

风险来源：民怨（主因）、稳定度不足、行星局部民怨、派系不满，
减去驻军压制；**首都有 -25% 的稳定加成**。

### 30.2 镇压

```bash
greyfall revolt --suppress 25
已镇压 卡帕门-52 的动乱（20,000 cr，军力 -300；民怨 +3%，民粹/劳工满意度 -6%）
```

代价是双重的：资源消耗 + **镇压本身激化矛盾**（民怨上升、民粹与劳工不满）。
这是「用铁腕换时间」，不是免费的解法。

### 30.3 派系最后通牒

当某派系**影响力 ≥ 30% 且满意度 ≤ 22%** 时，它会发出最后通牒：

| 回应 | 后果 |
|---|---|
| **让步**（30,000 cr） | 该派系满意度 +35%，但**其他派系 -10%**（资源是零和的） |
| **拒绝** | 该派系满意度 -25%，民怨 +8%，稳定 -6%，**政变风险 +12%** |

`greyfall revolt` 会显示各派系的**影响力 / 满意度 / 诉求压力 / 态势**，
并标出哪些「准备逼宫」。

### 30.4 查看动乱变化

推进后用 `revolt` 查看各星系阶段，再用 `domestic` 检查民怨、派系满意度与政变风险。
星系动乱和派系最后通牒有各自的条件，处理一项后仍需查看另一项是否消退。

---

## 31. 机制：间谍网络的进阶玩法

```bash
greyfall network                          # 网络总览 + 反渗透态势
greyfall network --establish 3            # 建立网络
greyfall network --agents 3 5             # 派 5 名特工
greyfall network --mission 3 意识形态渗透  # 执行任务
greyfall network --counter                # 反渗透：清剿境内外国网络
```

### 31.1 八种任务

| 任务 | 门槛 | 经费 | 效果 |
|---|---|---|---|
| 侦察 | 10% | 200 | 获取目标国力/军力/经济 |
| 煽动 | 25% | 600 | 目标民怨 +9% |
| 窃取科技 | 40% | 800 | 获得研究点 |
| 破坏 | 55% | 1,200 | 目标经济 -6%、稳定 -4% |
| 假情报 | 60% | 1,400 | 挑动目标对**第三方**的观感 -18% |
| 潜伏 | 65% | 1,500 | 渗透度 +18% |
| **意识形态渗透** | 70% | 1,800 | 长期改变目标社会 |
| **反渗透** | 20% | 1,000 | 清剿境内外国网络（对**本国**执行） |

反渗透门槛刻意压低（20%）——它是防御性任务，不该要求先渗透自己。

### 31.2 意识形态渗透

它不会立刻见效，而是累积**意识形态压力**（每季自然衰减 3%）：

| 压力 | 后果 |
|---|---|
| ≥ 40% | 与渗透方政体**伦理相近的派系**满意度与影响力上升，相远的下降；目标民怨缓慢上升 |
| ≥ 75% | **目标采纳渗透方的一项伦理**（改写其官方立场），压力回落到 30% 重新积累 |

「伦理相近」的判定：威权政体亲近军部/旧贵族/原教旨；民主共和亲近劳工/民粹/商会。

**这是唯一能从根本上改变对手的间谍手段**——其余任务都是一次性打击。

### 31.3 反渗透

```bash
greyfall network --counter
反渗透行动完成：清剿 2 个外国网络（双环联邦、塔兰联合体）
  ——渗透度 -30%、暴露 +35%（反间谍 +8%）
```

代价 15,000 cr，收益是清剿**所有**指向我国的外国网络。

**被破获的网络会保留 12 季**（标记「已破获」）再移除——
这样你才能看到境内曾有谁在活动、判断反间谍投入是否有效。

### 31.4 攻防节奏

网络的生命周期由**暴露度**决定：

```
t5   渗透 20%  暴露 74%
t10  渗透 41%  暴露 72%
t15  渗透 40%  暴露 84%
t20  破产（被破获）
```

渗透越深、特工越多，暴露涨得越快；不执行任务时会自然降温。
**高价值任务（破坏、意识形态）的暴露代价最高**，用一次就可能暴露整张网。

---

## 32. 机制：种族、奴役、太空生物与基因改造

```bash
greyfall species                                    # 总览
greyfall species --hunt 28                          # 猎杀太空生物
greyfall species --can 5                            # 加工海星罐头
greyfall species --labor 蓄奴制                     # 切换劳役制度
greyfall species --gene 睿智                        # 基因改造
greyfall species --press 3 --kind 经济施压          # 外交施压
```

### 32.1 太空生物

星系中栖息着四种巨型生物，开局播撒（约每 8 星系 1 群）：

| 生物 | 所需军力 | 猎杀所得 |
|---|---|---|
| **海星** | 300 | 400 食物 |
| 晶簇虫群 | 900 | 120 超材料 |
| 虚空鲸 | 1,400 | 180 奢侈品 |
| 裂隙潜行者 | 2,200 | 60 文物 |

**海星罐头**：每批投入 200 食物、20 能源、10 聚合物及 800 cr，施工 3 季后产出 30 奢侈品。每国同时限一个订单，每单 1～10 批；每批每季另需 100 cr、能源 5、部件 1。用 `queue` 查看进度，停工和取消遵守国家工程规则。

```
greyfall species --hunt 28
猎杀 海星：获得 156 单位食物（军力 -45，经费 -6,000 cr）

greyfall species --can 2
2 批订单：启动食物 400、能源 40、聚合物 20、1600 cr；3 季后获得奢侈品 60，另付施工补给。
```

猎杀有代价：军力损耗（威胁值的 15%）+ 6,000 cr。种群会自然增长（每 4 季 +4%）。

### 32.2 奴役

| 制度 | 产出 | 民怨增量 |
|---|---|---|
| 自由民 | ×1.00 | +0% |
| **种姓制** | ×1.15 | +8% |
| **蓄奴制** | ×1.30 | +20% |

切换需 20,000 cr。**蓄奴制的代价**：

- 民怨目标 +20%
- 劳工与民粹派系满意度 **-12%**
- **全体第三方观感 -15%**，且每 8 季继续下降
- 旧贵族与商会满意度 +8%

启用后观察收入与民怨的变化，资源收益和国内反应会同时结算。
倍率作用于**当季收入流量**，不会复利累积。

游牧公民特质可用 `greyfall species --migrate <本国星系>` 迁都。启动需要 300 影响力、10,000 cr、能源 100、部件 50；工期为 `4 + 距旧首都跳数` 季，每季维护 500 cr、能源 15。同时只能迁往一处，完工才更换首都，不会额外提高稳定度。

### 32.3 基因改造

先完成至少两项生物科技，再花凝聚力、40,000 cr、药品 150、生物制品 100 启动改造。工期 8 季，同时只能开展一项；每季维护 750 cr，另需药品 5、生物制品 3。完工后永久改写本国种族：

| 方向 | 凝聚力 | 效果 |
|---|---|---|
| 强健 | 1,500 | 人口增长 +20% |
| 勤勉 | 1,800 | 建造速率 +15% |
| 睿智 | 2,100 | 研究速率 +15% |
| 坚韧 | 2,400 | 稳定度 +12% |
| 温顺 | 2,700 | 民怨 -15% |

每项只能改造一次；已有改造每 12 季产生 1,200 cr 基因库维护费。
改造完成时会触怒**原教旨派系**（-10%），技术官僚则支持（+8%）。

### 32.4 外交施压

不宣战也能迫使对方让步：

```bash
greyfall species --press 3 --kind 经济施压
对 伽兰帝国 实施【经济施压】：压力 +18%（现 18%），对方观感 -8%
```

三种方式：**经济施压 / 军事施压 / 外交孤立**。成本 8,000 cr + 150 影响力。

- **军力优势放大效果**：每领先一倍军力，额外 +10% 压力
- 施压必然损害关系（对方观感 -8%）
- 压力每季衰减 5%
- **压力 ≥ 60% 时对方被迫进贡**：每 10 季支付其国库的 10%（上限 50,000 cr），随后压力回落

---

## 33. 机制：建造工期、政体与腐败

### 33.1 建造与造舰需要时间

**建筑需要施工时间**。动工时付清信用点和资源成本，然后逐季推进：

```bash
greyfall planets                  # 查询自己的行星编号
greyfall build 聚变电厂 62         # 示例编号，替换为自己拥有的行星
greyfall queue                    # 查看在建项目、剩余季数与总工期
```

| 规则 | 说明 |
|---|---|
| 工期 | 建筑基础工期随层级递增：1～4 级分别为 4、7、10、13 季 |
| 并行 | 每个行星同时最多 **2 项**在建 |
| 速度 | 开工时按速率和稳定度计算总工期，向上取整；此后每季减少 1 季，不重复加速。AI 同样排队施工 |
| 取消 | `greyfall queue --cancel <行星> --index n`，只退还已付信用点的 **40%**，资源不退 |

**造舰同样需要工期与设施**：

| 舰体 | 基础工期 |
|---|---|
| 护卫舰 | 2 季 |
| 护卫舰-重型 | 5 季 |
| 驱逐舰 | 8 季 |
| 巡洋舰 | 11 季 |
| 战列舰 | 14 季 |
| 泰坦 | 17 季 |

**首都星系天然具备造舰能力**；其他星系需要在本国行星上建造【轨道船坞】或【轨道船台】等提供造舰能力的设施。
船坞被摧毁或星系失守时工期**暂停**（不退款），恢复本国造舰能力后继续。建筑和舰船均按资源表全额扣款。

```bash
greyfall ship-build 0 25          # 示例：设计 0，在具备造舰能力的我方星系 25 开工
```

### 33.2 政体的经济与外交取舍

18 种政体中，以下四种对生产、民生开支、外交与腐败有不同影响：

| 政体 | 工厂效率 | 民生开销 | 正当化 | 关系改善 | 腐败倾向 |
|---|---|---|---|---|---|
| **社会主义** | **+25%** | **+30%** | — | +8% | -4% |
| **法西斯主义** | +12% | -8% | **+40%** | -25% | +2% |
| **资本主义** | +5% | -5% | -10% | +10% | **+30%** |
| **民主主义** | — | +6% | -15% | **+30%** | -12% |

- **社会主义**：工厂产出极高，但福利与民生开支同样沉重（维护费 +30%）
- **法西斯主义**：镇压机器高效，且**战争理由获得速度 +40%**（正当化几乎不费力），代价是外交信誉崩塌
- **资本主义**：市场繁荣、关系尚可，但**腐败深入骨髓**
- **民主主义**：决策依赖议会（政策过渡更慢、正当化更慢），但**外交关系改善能力最强**

### 33.3 腐败

**帝国规模的真实代价**：

```bash
greyfall corruption
 当前腐败        20.0%
 收入侵蚀        -20.0%
 民怨增量        +3.0%
 政体倾向        +30%（资本主义）
 规模            5 行星 / 2 星系
```

- 腐败度随**行星数与星系数**增长，每 4 季结算一次
- **直接按比例抽走净收入**（30% 腐败 = 收入少 30%）
- 同时推高民怨（腐败度 × 15%）
- 政体倾向差异巨大（见上表）

用 `corruption` 查看当前腐败程度及其对收入和民怨的影响。

**治理**：

```bash
greyfall corruption --purge
反腐运动完成：腐败 20% → 0%（25,000 cr；旧贵族/辛迪加/商会 -8%，劳工/民粹 +6%）
```

反腐会**触怒既得利益派系**，且腐败会随时间回升——这是持续开销，不是一次性解决。

### 33.4 开局随机化

新开纪元时，**你的国家也参与随机生成**，包括国名、种族与政体。
开局后用 `status`、`government`、`species` 查看实际结果。
希望重复同一开局时，保留种子、难度、帝国数、星系数等参数，并使用同一版本程序。

---

## 34. 机制：胜利条件

### 34.1 征服与治理条件

| # | 条件 | 门槛 |
|---|---|---|
| 一 | 击败所有对手 | 除玩家外无存活帝国，包含当前盟友或中立国 |
| 二 | 民怨 | ≤ 10% |
| 三 | 稳定度 | ≥ 85% |
| 四 | 合法性 | ≥ 85% |
| 五 | 派系平均满意度 | ≥ 75% |
| 六 | 每个派系的满意度 | 均 ≥ 60%，不能用高平均值掩盖某一派系的不满 |
| 七 | 财政 | 国库与本季经营净收入均 ≥ 0；借款只能补现金，不能消除经营亏损 |
| 八 | 基本物资 | 季末的能源、食物、药品库存分别不少于各自 2 季需求 |
| 九 | 国内秩序 | 无不安、叛乱或割据星系，无政变倒计时，无未处理的国内派系诉求 |
| 十 | 连续维持 | 前九项在季末连续达标，所需季数随难度变化 |

玩家自身必须存活并拥有领土。季末检查发生在事件、战争、财政和资源消费全部结算之后。

| 难度 | 1 | 2 | 3 | 4 | 5 |
|---|---|---|---|---|---|
| 连续达标季数 | 12 | 15 | 18 | 21 | 24 |

**任一季末条件不达标，连续季数归零**，重新达标后从第 1 季开始。
查询状态、反复运行 `victory` 或同季连续安抚派系，都不会增加已维持季数。
`defer` 只推迟派系诉求，仍需真正处理后才能通过国内秩序检查。

```bash
greyfall victory      # 查看各项进度与缺口
```

### 34.2 如何达成内政条件

| 条件 | 主要手段 |
|---|---|
| 民怨 | 补足物资短缺、福利国家（−18%）、减少伦理冲突、提高稳定性 |
| 稳定度 | 生态经济（+15%）、退伍军人保障法（+10%）、**开发行星**（最高 +12%）、压低民怨 |
| 合法性 | 稳定度、反腐法案、普选权法案、**开发行星** |
| 派系满意度 | 繁荣度（低民怨 + 高稳定 + 高开发度）与定向政策；同时照顾最低满意度的派系 |
| 财政与物资 | 控制维护费、保留生产能力、补足能源/食物/药品储备，观察季末库存 |
| 国内秩序 | 处理动乱和派系诉求，解除政变倒计时 |

政策可能同时让部分派系受益、让其他派系受损。
选择后用 `domestic` 检查平均满意度，并结合物资供应、稳定度与开发度改善国内状况。

### 34.3 发展循环

```
建设行星 → 开发度↑ → 贸易税收入↑、稳定度↑、合法性↑
        → 民怨↓、繁荣度↑ → 派系满意度↑ → 政变风险↓
        → 国家更稳 → 能支撑长期战争 → 击败所有对手
```

**开发度**每季增长：建筑提供动力、稳定度加速、民怨抑制、人口提供规模效应。上限 10。

### 34.4 检查胜利进度

```bash
greyfall victory       # 各项缺口、物资储备要求和连续达标季数
greyfall domestic      # 国内派系与民怨
greyfall effects       # 当前修正与来源
greyfall revolt        # 境内动乱与派系通牒
```

各项条件需要同时维持。最大难度下，即使当季指标全部合格，也要通过连续 24 季的治理考验。
短期补贴、借款、镇压或临时政策的效果可能在考验途中消退，用 `victory` 查看当季缺口。
失去全部星系与行星的帝国会覆灭；单纯停战、结盟或使对方成为附庸，不等于击败所有对手。

连续季数会随存档保存，读档后继续累计；新纪元从零开始。
旧存档没有连续治理记录时从零开始，但已取得的征服胜利会保留。达成征服胜利后，后续数值下降不会撤销成绩。

---

## 35. 机制：纪元与结局

### 35.1 结局向量

游戏追踪 8 个维度（可在 `conclusions` 或 `epoch --report` 中查看进度条）：

**霸权 / 联邦 / 资本 / 种族 / 知识 / 信仰 / 毁灭 / 超脱**

各维度的增长来自你的实际行为：扩张领土、缔结联邦、积累财富、推进科技、提交结论、传播信仰、发动战争、完成飞升等。

累计提交 40 个结论后，根据结局向量评估剧情结局，共 12 种。
第 200 季本身不会触发结局。剧情结局与征服胜利分别判定，剧情结局出现后仍可继续推进并挑战征服。
征服结局需要完成第 34 节的连续治理考验；无尽模式也会记录这一成绩。

### 35.2 幕

主线剧情分 7 幕。**提交足够多的本幕结论**即可推进到下一幕，每幕推进都会引起市场变化。

第 7 幕后还有隐藏的第 8 幕，需要满足特定条件才会开启。

### 35.3 纪元命令

```bash
greyfall epoch --report              # 当前纪元完整报告（含结局向量）
greyfall epoch --next                # 开启下一个纪元
greyfall epoch --next --inherit-legacy   # 开启并继承遗产
greyfall epoch --endless             # 切换无尽模式
greyfall epoch --seed <十六进制>      # 以指定种子重开
```

**继承遗产**会把以下内容带入新纪元：制度、债务、仇敌、技术、信誉，以及被攻破的心智模型（AI 从上一纪元就认识你）。

新纪元保留星系数与帝国数，难度提高一级，最高为 5。达到难度上限后仍可继续开启纪元。

---

## 36. 机制：存档与读档

### 36.1 存档槽

```bash
greyfall slots              # 列出所有存档槽
greyfall save --as <名字>    # 另存为新槽
greyfall load <名字>         # 载入某个槽
greyfall resume             # 恢复最近修改的槽
greyfall delete <槽> --yes   # 删除
greyfall prune --keep 8     # 只保留最近 8 个
```

推进 tick 时会自动存档，无需手动保存。

### 36.2 校验

```bash
greyfall verify <槽>         # 检查存档完整性
```

会检查文件头、校验和、加密认证、内容解码，以及防读档记录链的完整性。

### 36.3 导出与导入

```bash
greyfall export <槽> <文件路径>            # 导出
greyfall import <文件路径> <槽>            # 导入
greyfall export <槽> <路径> --plaintext    # 导出未加密状态（会提示破坏公平性）
```

### 36.4 读档与回退

```bash
greyfall rollback 2          # 回退 2 季
```

**回退会被记录，并且 AI 能看到。** 后果：

- AI 判定你会「重试」，从而在谈判中提高要价、降低让步概率
- 更倾向于对你采用不可逆的手段
- 你的信用评级会下降

查看记录：

```bash
greyfall chronicle            # 查看完整的操作记录链
greyfall chronicle --last 20
```

如果记录链文件丢失，会被判定为「档案焚毁」——所有阵营将视你为背约者，贸易与援助的优惠消失。

### 36.5 历史与重放

```bash
greyfall history --ticks 40      # 国力与国库的历史曲线
greyfall replay --from 7         # 从某个时间点重放并比对
```

---

## 37. 命令速查表

### 生命周期

| 命令 | 用途 |
|---|---|
| `help [--short]` | 总览帮助 |
| `man <命令>` | 单命令手册 |
| `version` | 版本信息 |
| `new [--seed S] [--difficulty 1..5] [--empires 8..16] [--systems 24..256] [--slot 名字] [--endless] [--force]` | 开新局 |
| `status` | 一屏概览 |
| `selftest [--verbose]` | 内置自检 |
| `verify <槽>` | 校验存档 |
| `slots` | 列出存档槽 |
| `resume` | 恢复最近进度 |

### 存档

| 命令 | 用途 |
|---|---|
| `save --as <槽>` | 另存为 |
| `load <槽>` | 载入 |
| `rollback <n>` | 回退 n 季 |
| `export <槽> <路径> [--plaintext]` | 导出 |
| `import <路径> <槽>` | 导入 |
| `prune --keep N` | 剪枝旧槽 |
| `delete <槽> --yes` | 删除 |
| `chronicle [--last N]` | 查看操作记录链 |

### 世界

| 命令 | 用途 |
|---|---|
| `overview` | 世界总览 |
| `starmap [--system X]` | 星图 |
| `sectors` | 星区列表 |
| `planet <id>` | 行星详情 |
| `empires [--filter alive\|ai]` | 阵营列表 |
| `relations [--empire X]` | 关系矩阵 |
| `treaties` | 条约列表 |
| `federation` | 联邦与投票 |
| `domestic` | 国内派系 |
| `tech [--branch b]` | 科技树 |
| `fleets [--id X]` | 舰队列表 |
| `ship [<设计>]` | 舰船设计 |
| `buildings [--planet P]` | 建筑与巨构 |
| `logs [--last N] [--phase P]` | 事件日志 |
| `history [--ticks N]` | 历史曲线 |
| `replay --from <tick>` | 重放比对 |

`--phase` 可选值：`setup` `ap` `pending` `market` `vol` `reader` `model` `ai` `federation` `domestic` `combat` `event` `clue` `plot` `economy` `tick` `save` `chronicle`

### 市场

| 命令 | 用途 |
|---|---|
| `market --res <商品> [--depth N]` | 盘面速览 |
| `book <交易所> <商品> [--depth N]` | 订单簿 |
| `quote <商品>` | 跨所报价 |
| `curve <商品>` | 期货期限结构 |
| `position` | 持仓 |
| `pnl [--ticks N]` | 盈亏 |
| `vol <商品>` | 波动率 |
| `arb [--net]` | 跨所套利 |
| `shock [--last N]` | 冲击记录 |
| `credit` | 信用与债务 |
| `fx` | 汇率 |
| `blackmarket` | 黑市 |
| `order buy\|sell <商品> <数量> [@ <价格>] [--exch X] [--tif day\|gtc] [--kind limit\|market\|iceberg] [--shown N]` | 下单 |
| `cancel <订单号>` | 撤单 |
| `modify <订单号> [@ <价格>] [<数量>]` | 改单 |
| `futures buy\|sell <商品> <F1..F4> <数量> [--lev n]` | 期货 |
| `settle` | 交割与结算 |
| `borrow <金额> [--term N]` | 借款 |
| `repay [--amount A]` | 还款 |
| `escrow open\|release <阵营\|编号> <金额> [--term N]` | 托管 |
| `insure <商品> [--cover N]` | 保险 |

### 外交与情报

| 命令 | 用途 |
|---|---|
| `envoy <阵营> <动作> [--terms ...]` | 外交 |
| `spy <阵营> --mission <任务> --budget n` | 间谍 |
| `gift <阵营> <商品> <数量>` | 赠礼 |
| `intel [--threat\|--what-they-know\|--counterintel\|--decoy] [--empire X] [--signal S]` | 情报与误导 |
| `propaganda <目标> <叙事>` | 舆论战 |

`spy --mission` 可选值：`intel` `read-mind` `insider` `exfil` `sabotage` `forgery`

### 道具

| 命令 | 用途 |
|---|---|
| `inventory [--filter TAG]` | 库存 |
| `item [<id>] [--explain]` | 道具详情 |
| `combine <A> <B>` | 合成 |
| `disassemble <道具>` | 拆解 |
| `use <道具> [--target X]` | 使用 |
| `forge-prove <道具>` | 造假冒牌 |
| `equip <模块> <舰队>` | 装配 |

### 线索

| 命令 | 用途 |
|---|---|
| `clues [--tag T] [--unlinked] [--subject X]` | 线索列表 |
| `link <A> <B> [--kind ...]` | 连接 |
| `unlink <A> <B>` | 断开 |
| `archive <线索>` | 归档 |
| `deduce [--conclusion X] [--explain\|--commit]` | 推断 |
| `conclusions` | 已提交结论与结局向量 |

`--kind` 可选值：`corroborate` `contradict` `prereq` `belongs` `about`

### 国家运营

| 命令 | 用途 |
|---|---|
| `colony [<星系> / --detail <星系>]` | 殖民容量、报价与启动 |
| `queue [--cancel-project <项目号>]` | 查看建筑、造舰和国家工程，取消国家工程 |
| `ship-build <设计> <星系>` | 建造舰船 |
| `fleet --id X --order <命令> [--to <星系>]` | 舰队命令 |
| `edict [<法令>]` | 法令 |
| `research <分支\|科技>` | 研究 |
| `build <建筑> <行星>` | 建造 |
| `mega <巨构> <星系>` | 巨构 |
| `ascend [<路径>]` | 飞升 |
| `recruit` | 招募 |

### 军事

| 命令 | 用途 |
|---|---|
| `battles [--empire X]` | 战斗态势 |
| `front [--detail <emp>]` | 战线总览 |
| `commanders [--recruit [--trait T]\|--assign <c>:<f>]` | 指挥官 |

### 科技 / 政策 / 决议 / 谈判 / 胜利

| 命令 | 用途 |
|---|---|
| `policy [--enact <政策>\|--detail <政策>]` | 政策 |
| `parliament [--propose\|--vote\|--persuade\|--force]` | 议会立法 |
| `resolve [--detail <id>\|--activate <id>\|--available]` | 决议系统 |
| `negotiate <emp> [--status\|--demand ...\|--offer ...]` | 谈判 |
| `victory` | 胜利条件进度 |

### 推进与纪元

| 命令 | 用途 |
|---|---|
| `advance [--ticks N]` | 推进（有抉择时返回 5） |
| `choose <序号> [<选项>] [--option N]` | 仅给序号时查看选项，给出选项时结算抉择 |
| `defer [<序号>] [--ticks N]` | 延后抉择 |
| `epoch --report\|--next [--inherit-legacy]\|--seed <hex>\|--endless` | 纪元管理 |

---

## 附：最小可玩流程

```bash
# 开局
greyfall new --seed 5EED-C0FFEE --difficulty 3 --empires 12
greyfall status
greyfall overview

# 看看市场
greyfall market --res alloys --depth 8
greyfall quote alloys

# 交易
greyfall order buy alloys 1200 @31.4 --tif gtc
greyfall position

# 外交
greyfall empires
greyfall envoy 1 propose-trade
greyfall intel --what-they-know --empire 1

# 推进；若返回退出码 5，先按下面的事件处理步骤操作
greyfall advance --ticks 1

# 运营
greyfall research 工程
greyfall starmap
greyfall domestic

# 查看进展与结局
greyfall conclusions
greyfall epoch --report
```

遇到待抉择时，先查看再选择：

```bash
greyfall status
greyfall choose 0         # 查看选项
greyfall choose 0 1       # 示例：选择选项 1，请按事件内容决定
greyfall advance --ticks 1
```

殖民时，先运行 `greyfall colony` 检查容量，再从星图选出邻接目标，用 `greyfall colony --detail <星系编号>` 查看总投入，备齐资金与物资后用 `greyfall colony <星系编号>` 启动，逐季查看 `queue` 补给和进度。
编号随开局变化，命令中的尖括号表示需要替换的参数，不要原样输入。
